---
remote_endpoint: https://testnet.movementnetwork.xyz/
name: "disable-concurrent-fungible-balance"
proposals:
  - name: disable_concurrent_fungible_balance
    metadata:
      title: "Disable concurrent fungible balance"
      description: "This is for disabling concurrent fungible balance"
    execution_mode: RootSigner
    update_sequence:
      - Framework:
          bytecode_version: 7
          git_hash: ~
      - FeatureFlag:
          enabled: []
          disabled:
            - concurrent_fungible_balance
