[package]
name = "AptosExperimental"
version = "1.0.0"

[addresses]
# Assigned at compile/publish time (e.g. `0x7` for releases/tests, profile account for local publish).
aptos_experimental = "_"

[dependencies]
AptosFramework = { local = "../aptos-framework" }
