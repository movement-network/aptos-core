# Difftest inventory — Confidential assets (experimental)

**Independent / gold vectors (§4.3 iii):** scaffold [`../corpora/confidential_assets/README.md`](../corpora/confidential_assets/README.md) — populate to strengthen peer-reviewable evidence beyond VM↔same-repo Lean.

**Native / crypto map (FV Workstream A):** [`confidential_native_matrix.md`](confidential_native_matrix.md).

**Move sources:** `aptos-move/framework/aptos-experimental/sources/confidential_asset/`  
**Modules:** `confidential_asset`, `confidential_proof`, `confidential_balance`, `ristretto255_twisted_elgamal` (+ `confidential_gas_e2e_helpers` for tests)  
**Rust suite ids (registered):** `global_resource_smoke`, `confidential_balance`, `confidential_elgamal`, `confidential_proof`, `confidential_asset`, **`fa_stub`** (FA `MachineState` stub alignment), meta **`confidential`** (the four CA-related suites + `fa_stub`; `global_resource_smoke` is separate).  
**Lean:** `MovementFormal.MoveModel.Programs.Confidential` + `DiffTest.Runner` mappings for the oracle function names listed in those suites.

## 1. Testing discipline (confidential assets are not assumed correct)

- **Oracle** = output of the **Move VM** on pinned bytecode + inputs. If CA Move has a bug, the oracle records **that** behavior.
- **Lean** is a **second implementation** of the same *intended* semantics (`Move.step` + natives). When both are wired, **disagreement fails CI** — you then determine whether Move, Lean transcription, or Lean natives are wrong.
- **Regression workflow:** after a **deliberate** Move fix, **regenerate** oracles; if Lean was matching old buggy behavior, tests fail until Lean is fixed — that is **desirable**.

Do **not** hand-edit oracle JSON to “match Lean” without a VM run.

## 2. Transitive natives / external modules (closure for proof + crypto paths)

The CA `.move` files under `confidential_asset/` declare **no** `native fun` themselves; execution relies on:

| Dependency | Used for (typical) |
| ---------- | ------------------ |
| `aptos_std::ristretto255` | Points, scalars, mul/add/sub, decompress, … |
| `aptos_std::aptos_hash` | SHA3-512 (Bulletproofs DST digest); Fiat–Shamir challenges use `ristretto255::new_scalar_from_sha2_512` |
| `aptos_std::ristretto255_bulletproofs` | Range proof verify / prove paths |
| `std::vector`, `std::option`, `std::bcs` (if any) | Structure per compiler |
| `aptos_framework::*` / FA / object | **Entrypoints** in `confidential_asset` (deposits, transfers, …) |

**Lean implication:** every native on a **VM↔Lean** path needs a Lean binding or a documented **VM-only** cut.

## 3. `confidential_asset` — entrypoints & key `public fun`

| Symbol | Kind | Observable | Planned mode | Notes |
| ------ | ---- | ----------- | ------------ | ----- |
| `register` | `entry` | state + events + abort | Blocked | FA + global store |
| `deposit_to` / `deposit` / `deposit_coins*` | `entry` | same | Blocked | FA transfer + store |
| `withdraw_to` / `withdraw` | `entry` | same | Blocked | proofs + FA |
| `confidential_transfer` | `entry` | same | Blocked | sigma + Bulletproofs + FA |
| `rotate_encryption_key` / `normalize` | `entry` | same | Blocked | proofs + store; merged e2e oracle rows **`confidential_asset_rotate_encryption_key_and_unfreeze_only`**, **`confidential_asset_rollover_then_normalize_only`** (VM runs real `verify_*`; Lean **witness** only) |
| `freeze_token` / `unfreeze_token` | `entry` | same | Blocked | store |
| `rollover_pending_balance` / `…_and_freeze` / `rotate_encryption_key_and_unfreeze` | `entry` | same | Blocked | store |
| `enable_allow_list` / `disable_allow_list` / `enable_token` / `disable_token` / `set_auditor` | `public fun` | store | Blocked | governance + `signer` |
| `has_confidential_asset_store` / `is_token_allowed` / … | `public fun` | reads | Blocked | global reads |
| `pending_balance` / `actual_balance` / `encryption_key` | `public fun` | returns | Blocked | store |
| `register_internal` / `deposit_to_internal` / `withdraw_to_internal` / `confidential_transfer_internal` / … | `public fun` | | **VM↔Lean candidate** | Prefer **internal** paths first (Phase 1–4 plan) when store can be stubbed |
| `serialize_auditor_eks` / `serialize_auditor_amounts` | `public fun` | `vector<u8>` | VM↔Lean: `0x1::difftest_confidential_asset_layer` calls the real helpers (empty + non-empty EKs **32 / 64 / 96 / 128 / 160 / 192** B; pending/actual wires including **512**/**768** B permutations; corpora under [`corpora/confidential_assets/`](../corpora/confidential_assets/)). |

## 4. `confidential_proof`

| Symbol | Kind | Observable | Planned mode | Notes |
| ------ | ---- | ----------- | ------------ | ----- |
| `verify_registration_proof` | `public(friend)` | abort / unit | VM↔Lean (later) | Friend-only; harness uses **`verify_registration_proof_for_difftest`** (normal `public`) |
| `verify_registration_proof_for_test` | `public` + `#[test_only]` | abort / unit | VM-only in `head.mrb` | Prefer **`verify_registration_proof_for_difftest`** for `0x1` harnesses |
| `verify_registration_proof_for_difftest` | `public` | abort / unit | VM↔Lean candidate | Thin wrapper for difftest / tooling (oracle **171** with Lean **`execVerifyRegistrationProof`** column) |
| `prove_registration_deterministic_for_difftest` | `public` | `(vector<u8>, vector<u8>)` | VM↔Lean candidate | Deterministic nonce `k`; pairs with **`verify_registration_proof_for_difftest`** |
| `registration_fs_message_for_test` | `public` | `vector<u8>` | VM↔Lean candidate | Aligns with existing Lean goldens |
| `difftest_registration_helpers::{registration_sha2_512_golden_move_first,registration_sha2_512_golden_move_second}` (via `difftest_confidential_proof` harness) | `public` | `vector<u8>` **64** B | **VM↔Lean** | Matches hex **`registration_sha2_512_golden_{1,2}.hex`** and Lean **`TranscriptAlignment`** / **`verify-corpora`**; oracle indices **174** / **175** (`Programs/Confidential`) |
| `verify_withdrawal_proof` / `verify_transfer_proof` / `verify_normalization_proof` / `verify_rotation_proof` | `public` | abort / unit | **Negative pin only** (Phase D.1, Lean **195**) | Happy path still needs Bulletproofs + sigma natives (Phase D.2/D.3); reject direction covered by four `test_verify_*_proof_zero_sigma_aborts` rows pinning `ESIGMA_PROTOCOL_VERIFY_FAILED` (**65537**). Required `BULLETPROOFS_NATIVES` (24) + `BULLETPROOFS_BATCH_NATIVES` (87) feature bits enabled in the difftest on-chain `0x1::features::Features` resource (otherwise `point_clone`/`double_scalar_mul` natives abort with `E_NATIVE_FUN_NOT_AVAILABLE` **196613** before the sigma check runs). |
| `prove_*` / `serialize_*` / `deserialize_*` | `public` | various | VM-only or VM↔Lean | Case-by-case; `prove_*` may be `test_only` — confirm in source |
| `get_fiat_shamir_*` / `get_bulletproofs_*` | `public` | constants | VM↔Lean candidate | Trivial oracle rows |

## 5. `confidential_balance`

| Symbol | Kind | Observable | Planned mode | Notes |
| ------ | ---- | ----------- | ------------ | ----- |
| `new_*_no_randomness`, `new_*_from_bytes`, `compress_balance`, `decompress_balance`, `balance_to_bytes` | `public` | values / `Option` | VM↔Lean candidate | Good **Phase 2** targets (crypto + structure) |
| `add_balances_mut` / `sub_balances_mut` | `public` | mut ref effect | VM↔Lean candidate | Encode as **function** return in oracle (copy-out pattern) |
| `balance_equals` / `balance_c_equals` / `is_zero_balance` | `public` | `bool` | VM↔Lean candidate | |
| `split_into_chunks_u64` / `split_into_chunks_u128` | `public` | `vector<Scalar>` internally | VM↔Lean | Harness tests (`test_split_into_chunks_*_first_chunk`) return **`bool`** (scalar check inside Move); oracle carries **no** `vector<Scalar>` — no JSON schema extension required for these rows. |
| `get_*_chunks` / `get_chunk_size_bits` | `public` | `u64` | VM↔Lean candidate | trivial |
| `generate_balance_randomness` / `new_*_from_u128` / `verify_*` | `test_only` / `public` | | VM-only | Mark test-only paths in harness |

## 6. `ristretto255_twisted_elgamal`

| Area | Observable | Planned mode | Notes |
| ---- | ----------- | ------------ | ----- |
| Ciphertext / pubkey **constructors**, `ciphertext_add*`, `compress*`, `decompress*`, `ciphertext_equals`, … | points / bool / bytes | VM↔Lean candidate | Foundation for balance proofs |

## 7. `confidential_gas_e2e_helpers`

| Note |
| ---- |
| Test / packaging helpers — **lower** difftest priority unless product depends on them. |

## 8. Concrete oracle cases (VM → JSON → Lean)

| Case id (Rust label) | Target | Inputs (summary) | Expected (summary) | Mode |
| --------------------- | ------ | ---------------- | ------------------ | ---- |
| `const` / `len` / `bool` / `roundtrip` / … | `confidential_balance` chunk sizes, zero serialization lengths, `is_zero_*`, compress/decompress, `Option` parse edges, `add_*` on zeros | fixed / empty vectors | `u64`, `bool`, `vector<u8>`, `Option` | VM↔Lean (Lean native stubs) |
| `eq_self` / `eq_c_self` / `eq_two0` / `sub_zero` | `balance_equals`, `balance_c_equals`, `sub_balances_mut` on zero pending balances | none | `bool` (`true`) | VM↔Lean (VM full crypto; Lean index 47–50 = `ldTrue`) |
| `dst` / `bits` / `*_empty_none` | `confidential_proof` constants + `deserialize_*` on empty input | none / empty `vector<u8>` | `vector<u8>` / `Option` | VM↔Lean (stubs) |
| `*_layout_some` | `deserialize_{withdrawal,normalization,rotation,transfer}_*` returns **`Some`** on VM-built sigma (canonical **0** scalar + **A_POINT** per slot) + empty ZKRP byte vectors | fixed layout lengths | `bool(true)` | **VM:** real parsers. **Lean:** **110–113** — same **`Step`** as **128–130** (`ldConst` **24–26** + `vecLen` + `eq`; necessary **length** on corpus sigma bytes, not `deserialize_*` / `verify_*` in `eval`). **Corpora:** [`deserialize_sigma_18_scalars_18_points.hex`](../corpora/confidential_assets/deserialize_sigma_18_scalars_18_points.hex), [`deserialize_sigma_19_scalars_19_points.hex`](../corpora/confidential_assets/deserialize_sigma_19_scalars_19_points.hex), [`deserialize_sigma_transfer_26_scalars_30_points.hex`](../corpora/confidential_assets/deserialize_sigma_transfer_26_scalars_30_points.hex). |
| `sigma18_len` / `sigma19_len` / `sigma_tr_len` | VM `layout_sigma_*().length()` vs **1152** / **1216** / **1792** | none | `bool(true)` | VM↔Lean (**funcIdx 128–130**): **`ldConst`** same bytes as corpora + **`vecLen`** + **`eq`** (real `Step`; same bytecode pattern as **110–113** for the matching sigma layout). |
| `sigma_tr_ext1920_len` | VM `layout_sigma_transfer_one_auditor_quad_extension().length() == 1920` | none | `bool(true)` | VM↔Lean (**131**): **`ldConst` 27** + `vecLen` + `eq`; corpus [`deserialize_sigma_transfer_26_scalars_30_points_plus_one_auditor_quad.hex`](../corpora/confidential_assets/deserialize_sigma_transfer_26_scalars_30_points_plus_one_auditor_quad.hex). |
| `tr_ext_layout_some` | `deserialize_transfer_proof` returns **`Some`** on base sigma + **4×A_POINT** extension + empty ZKRP vectors | **1920**-byte sigma | `bool(true)` | **VM:** real parser (`auditor_xs = 128`). **Lean:** **132** — same **`Step`** as **131** (necessary **length**). |
| `sigma_tr_ext2048_len` | VM `layout_sigma_transfer_two_auditor_quads_extension().length() == 2048` | none | `bool(true)` | VM↔Lean (**133**): **`ldConst` 28** + `vecLen` + `eq`; corpus [`deserialize_sigma_transfer_26_scalars_30_points_plus_two_auditor_quads.hex`](../corpora/confidential_assets/deserialize_sigma_transfer_26_scalars_30_points_plus_two_auditor_quads.hex). |
| `tr_ext2_layout_some` | `deserialize_transfer_proof` **`Some`** on base + **8×A_POINT** + empty ZKRP | **2048**-byte sigma | `bool(true)` | **VM:** `auditor_xs = 256`. **Lean:** **134** = **133**. |
| `sigma_tr_ext2176_len` | VM `layout_sigma_transfer_three_auditor_quads_extension().length() == 2176` | none | `bool(true)` | VM↔Lean (**135**): **`ldConst` 29** + `vecLen` + `eq`; corpus [`deserialize_sigma_transfer_26_scalars_30_points_plus_three_auditor_quads.hex`](../corpora/confidential_assets/deserialize_sigma_transfer_26_scalars_30_points_plus_three_auditor_quads.hex). |
| `tr_ext3_layout_some` | `deserialize_transfer_proof` **`Some`** on base + **12×A_POINT** + empty ZKRP | **2176**-byte sigma | `bool(true)` | **VM:** `auditor_xs = 384`. **Lean:** **136** = **135**. |
| `sigma_tr_ext2304_len` | VM `layout_sigma_transfer_four_auditor_quads_extension().length() == 2304` | none | `bool(true)` | VM↔Lean (**137**): **`ldConst` 30** + `vecLen` + `eq`; corpus [`deserialize_sigma_transfer_26_scalars_30_points_plus_four_auditor_quads.hex`](../corpora/confidential_assets/deserialize_sigma_transfer_26_scalars_30_points_plus_four_auditor_quads.hex). |
| `tr_ext4_layout_some` | `deserialize_transfer_proof` **`Some`** on base + **16×A_POINT** + empty ZKRP | **2304**-byte sigma | `bool(true)` | **VM:** `auditor_xs = 512`. **Lean:** **138** = **137**. |
| `sigma_tr_ext2432_len` | VM `layout_sigma_transfer_five_auditor_quads_extension().length() == 2432` | none | `bool(true)` | VM↔Lean (**139**): **`ldConst` 31** + `vecLen` + `eq`; corpus [`deserialize_sigma_transfer_26_scalars_30_points_plus_five_auditor_quads.hex`](../corpora/confidential_assets/deserialize_sigma_transfer_26_scalars_30_points_plus_five_auditor_quads.hex). |
| `tr_ext5_layout_some` | `deserialize_transfer_proof` **`Some`** on base + **20×A_POINT** + empty ZKRP | **2432**-byte sigma | `bool(true)` | **VM:** `auditor_xs = 640`. **Lean:** **140** = **139**. |
| `sigma_tr_ext2560_len` | VM `layout_sigma_transfer_six_auditor_quads_extension().length() == 2560` | none | `bool(true)` | VM↔Lean (**141**): **`ldConst` 32** + `vecLen` + `eq`; corpus [`deserialize_sigma_transfer_26_scalars_30_points_plus_six_auditor_quads.hex`](../corpora/confidential_assets/deserialize_sigma_transfer_26_scalars_30_points_plus_six_auditor_quads.hex). |
| `tr_ext6_layout_some` | `deserialize_transfer_proof` **`Some`** on base + **24×A_POINT** + empty ZKRP | **2560**-byte sigma | `bool(true)` | **VM:** `auditor_xs = 768`. **Lean:** **142** = **141**. |
| `sigma_tr_ext2688_len` | VM `layout_sigma_transfer_seven_auditor_quads_extension().length() == 2688` | none | `bool(true)` | VM↔Lean (**143**): **`ldConst` 33** + `vecLen` + `eq`; corpus [`deserialize_sigma_transfer_26_scalars_30_points_plus_seven_auditor_quads.hex`](../corpora/confidential_assets/deserialize_sigma_transfer_26_scalars_30_points_plus_seven_auditor_quads.hex). |
| `tr_ext7_layout_some` | `deserialize_transfer_proof` **`Some`** on base + **28×A_POINT** + empty ZKRP | **2688**-byte sigma | `bool(true)` | **VM:** `auditor_xs = 896`. **Lean:** **144** = **143**. |
| `sigma_tr_ext2816_len` | VM `layout_sigma_transfer_eight_auditor_quads_extension().length() == 2816` | none | `bool(true)` | VM↔Lean (**145**): **`ldConst` 34** + `vecLen` + `eq`; corpus [`deserialize_sigma_transfer_26_scalars_30_points_plus_eight_auditor_quads.hex`](../corpora/confidential_assets/deserialize_sigma_transfer_26_scalars_30_points_plus_eight_auditor_quads.hex). |
| `tr_ext8_layout_some` | `deserialize_transfer_proof` **`Some`** on base + **32×A_POINT** + empty ZKRP | **2816**-byte sigma | `bool(true)` | **VM:** `auditor_xs = 1024`. **Lean:** **146** = **145**. |
| `sigma_tr_ext2944_len` | VM `layout_sigma_transfer_nine_auditor_quads_extension().length() == 2944` | none | `bool(true)` | VM↔Lean (**147**): **`ldConst` 35** + `vecLen` + `eq`; corpus [`deserialize_sigma_transfer_26_scalars_30_points_plus_nine_auditor_quads.hex`](../corpora/confidential_assets/deserialize_sigma_transfer_26_scalars_30_points_plus_nine_auditor_quads.hex). |
| `tr_ext9_layout_some` | `deserialize_transfer_proof` **`Some`** on base + **36×A_POINT** + empty ZKRP | **2944**-byte sigma | `bool(true)` | **VM:** `auditor_xs = 1152`. **Lean:** **148** = **147**. |
| `sigma_tr_ext3072_len` | VM `layout_sigma_transfer_ten_auditor_quads_extension().length() == 3072` | none | `bool(true)` | VM↔Lean (**149**): **`ldConst` 36** + `vecLen` + `eq`; corpus [`deserialize_sigma_transfer_26_scalars_30_points_plus_ten_auditor_quads.hex`](../corpora/confidential_assets/deserialize_sigma_transfer_26_scalars_30_points_plus_ten_auditor_quads.hex). |
| `tr_ext10_layout_some` | `deserialize_transfer_proof` **`Some`** on base + **40×A_POINT** + empty ZKRP | **3072**-byte sigma | `bool(true)` | **VM:** `auditor_xs = 1280`. **Lean:** **150** = **149**. |
| `sigma_tr_ext3200_len` | VM `layout_sigma_transfer_eleven_auditor_quads_extension().length() == 3200` | none | `bool(true)` | VM↔Lean (**151**): **`ldConst` 37** + `vecLen` + `eq`; corpus [`deserialize_sigma_transfer_26_scalars_30_points_plus_eleven_auditor_quads.hex`](../corpora/confidential_assets/deserialize_sigma_transfer_26_scalars_30_points_plus_eleven_auditor_quads.hex). |
| `tr_ext11_layout_some` | `deserialize_transfer_proof` **`Some`** on base + **44×A_POINT** + empty ZKRP | **3200**-byte sigma | `bool(true)` | **VM:** `auditor_xs = 1408`. **Lean:** **152** = **151**. |
| `sigma_tr_ext3328_len` | VM `layout_sigma_transfer_twelve_auditor_quads_extension().length() == 3328` | none | `bool(true)` | VM↔Lean (**153**): **`ldConst` 38** + `vecLen` + `eq`; corpus [`deserialize_sigma_transfer_26_scalars_30_points_plus_twelve_auditor_quads.hex`](../corpora/confidential_assets/deserialize_sigma_transfer_26_scalars_30_points_plus_twelve_auditor_quads.hex). |
| `tr_ext12_layout_some` | `deserialize_transfer_proof` **`Some`** on base + **48×A_POINT** + empty ZKRP | **3328**-byte sigma | `bool(true)` | **VM:** `auditor_xs = 1536`. **Lean:** **154** = **153**. |
| `sigma_tr_ext3456_len` | VM `layout_sigma_transfer_thirteen_auditor_quads_extension().length() == 3456` | none | `bool(true)` | VM↔Lean (**155**): **`ldConst` 39** + `vecLen` + `eq`; corpus [`deserialize_sigma_transfer_26_scalars_30_points_plus_thirteen_auditor_quads.hex`](../corpora/confidential_assets/deserialize_sigma_transfer_26_scalars_30_points_plus_thirteen_auditor_quads.hex). |
| `tr_ext13_layout_some` | `deserialize_transfer_proof` **`Some`** on base + **52×A_POINT** + empty ZKRP | **3456**-byte sigma | `bool(true)` | **VM:** `auditor_xs = 1664`. **Lean:** **156** = **155**. |
| `sigma_tr_ext3584_len` | VM `layout_sigma_transfer_fourteen_auditor_quads_extension().length() == 3584` | none | `bool(true)` | VM↔Lean (**157**): **`ldConst` 40** + `vecLen` + `eq`; corpus [`deserialize_sigma_transfer_26_scalars_30_points_plus_fourteen_auditor_quads.hex`](../corpora/confidential_assets/deserialize_sigma_transfer_26_scalars_30_points_plus_fourteen_auditor_quads.hex). |
| `tr_ext14_layout_some` | `deserialize_transfer_proof` **`Some`** on base + **56×A_POINT** + empty ZKRP | **3584**-byte sigma | `bool(true)` | **VM:** `auditor_xs = 1792`. **Lean:** **158** = **157**. |
| `sigma_tr_ext3712_len` | VM `layout_sigma_transfer_fifteen_auditor_quads_extension().length() == 3712` | none | `bool(true)` | VM↔Lean (**159**): **`ldConst` 41** + `vecLen` + `eq`; corpus [`deserialize_sigma_transfer_26_scalars_30_points_plus_fifteen_auditor_quads.hex`](../corpora/confidential_assets/deserialize_sigma_transfer_26_scalars_30_points_plus_fifteen_auditor_quads.hex). |
| `tr_ext15_layout_some` | `deserialize_transfer_proof` **`Some`** on base + **60×A_POINT** + empty ZKRP | **3712**-byte sigma | `bool(true)` | **VM:** `auditor_xs = 1920`. **Lean:** **160** = **159**. |
| `sigma_tr_ext3840_len` | VM `layout_sigma_transfer_sixteen_auditor_quads_extension().length() == 3840` | none | `bool(true)` | VM↔Lean (**161**): **`ldConst` 42** + `vecLen` + `eq`; corpus [`deserialize_sigma_transfer_26_scalars_30_points_plus_sixteen_auditor_quads.hex`](../corpora/confidential_assets/deserialize_sigma_transfer_26_scalars_30_points_plus_sixteen_auditor_quads.hex). |
| `tr_ext16_layout_some` | `deserialize_transfer_proof` **`Some`** on base + **64×A_POINT** + empty ZKRP | **3840**-byte sigma | `bool(true)` | **VM:** `auditor_xs = 2048`. **Lean:** **162** = **161**. |
| `sigma_tr_ext3968_len` | VM `layout_sigma_transfer_seventeen_auditor_quads_extension().length() == 3968` | none | `bool(true)` | VM↔Lean (**163**): **`ldConst` 43** + `vecLen` + `eq`; corpus [`deserialize_sigma_transfer_26_scalars_30_points_plus_seventeen_auditor_quads.hex`](../corpora/confidential_assets/deserialize_sigma_transfer_26_scalars_30_points_plus_seventeen_auditor_quads.hex). |
| `tr_ext17_layout_some` | `deserialize_transfer_proof` **`Some`** on base + **68×A_POINT** + empty ZKRP | **3968**-byte sigma | `bool(true)` | **VM:** `auditor_xs = 2176`. **Lean:** **164** = **163**. |
| `sigma_tr_ext4096_len` | VM `layout_sigma_transfer_eighteen_auditor_quads_extension().length() == 4096` | none | `bool(true)` | VM↔Lean (**165**): **`ldConst` 44** + `vecLen` + `eq`; corpus [`deserialize_sigma_transfer_26_scalars_30_points_plus_eighteen_auditor_quads.hex`](../corpora/confidential_assets/deserialize_sigma_transfer_26_scalars_30_points_plus_eighteen_auditor_quads.hex). |
| `tr_ext18_layout_some` | `deserialize_transfer_proof` **`Some`** on base + **72×A_POINT** + empty ZKRP | **4096**-byte sigma | `bool(true)` | **VM:** `auditor_xs = 2304`. **Lean:** **166** = **165**. |
| `sigma_tr_ext4224_len` | VM `layout_sigma_transfer_nineteen_auditor_quads_extension().length() == 4224` | none | `bool(true)` | VM↔Lean (**167**): **`ldConst` 45** + `vecLen` + `eq`; corpus [`deserialize_sigma_transfer_26_scalars_30_points_plus_nineteen_auditor_quads.hex`](../corpora/confidential_assets/deserialize_sigma_transfer_26_scalars_30_points_plus_nineteen_auditor_quads.hex). |
| `tr_ext19_layout_some` | `deserialize_transfer_proof` **`Some`** on base + **76×A_POINT** + empty ZKRP | **4224**-byte sigma | `bool(true)` | **VM:** `auditor_xs = 2432`. **Lean:** **168** = **167**. |
| `fa_stub` | `difftest_fa_stub::test_fa_stub_balance_answer` | none | **`u64(12345)`** | VM↔Lean (**52**): **`faReadBalance`**; `Runner` seeds **`faBalances ((1,2) ↦ 12345)`** (see `STUB_POLICY.md` L5). |
| `fa_stub_write_read` | `difftest_fa_stub::test_fa_stub_write_then_read_balance` | none | **`u64(9999)`** | VM↔Lean (**169**): **`faWriteBalance`** then **`faReadBalance`** at `(meta=1, owner=2)` from **`MachineState.empty`**. |
| `fs_wd` / `fs_tr` / `fs_norm` / `fs_rot` | `get_fiat_shamir_*_sigma_dst()` on `confidential_proof` | none | `vector<u8>` (exact `FIAT_SHAMIR_*` prefixes) | VM↔Lean (`Programs/Confidential` const pool indices 43–46) |
| `fs_reg` | `get_fiat_shamir_registration_sigma_dst()` | none | `vector<u8>` | VM↔Lean (const pool index 8; func index 51) |
| `schnorr_helpers` | `test_registration_helpers_roundtrip` → `difftest_registration_helpers::registration_roundtrip_vm` | dk=42, k=9999 fixture | `bool(true)` | VM↔Lean (**35**): **`caRegistrationHelpersRoundtripNative`** (`Operational.execVerifyRegistrationProof` on `RegistrationDifftestOracle`). |
| `reg_fs_fw_eq_helpers` | `test_registration_fs_message_framework_matches_helpers_golden` | formal golden FS inputs | `bool(true)` | VM↔Lean (**170**): **`ldTrue`** stub (VM runs **`registration_fs_message_for_test`** vs helpers golden). |
| `reg_proof_fw_rt` | `test_registration_proof_framework_deterministic_verify_roundtrip` | same fixture as **`schnorr_helpers`** | `bool(true)` | VM↔Lean (**171**): same Lean native as **35**; VM runs **`prove_registration_deterministic_for_difftest`** + **`verify_registration_proof_for_difftest`**. |
| `reg_fs_golden` | `test_registration_fs_message_golden_move` | none | **161**-byte `vector<u8>` | VM↔Lean (**38**): `caRegistrationFsMsgGoldenDesc` vs `TranscriptAlignment` goldens. |
| `reg_fs_golden_2` | `test_registration_fs_message_golden_move_second` | none | **161**-byte `vector<u8>` | VM↔Lean (**172**): **`ldConst` 46** + `ret` vs **`expectedRegistrationFsMsg2`**. |
| `reg_fs_fw_eq_helpers_2` | `test_registration_fs_message_framework_second_scenario_matches_helpers_golden` | second formal golden inputs | `bool(true)` | VM↔Lean (**173**): **`ldTrue`** stub. |
| `smoke` / `act_chunks` / `chunk_bits` | `confidential_asset` layer re-exports (`get_pending_balance_chunks` / `get_actual_balance_chunks` / `get_chunk_size_bits`) | none | `u64` | VM↔Lean (Option **B** — no globals; `act_chunks` / `chunk_bits` share Lean indices **1** / **2**) |
| `eks` / `amounts` | real `confidential_asset::serialize_auditor_*` on empty vectors | none | empty `vector<u8>` | VM↔Lean (`0x1` harness → framework module) |
| `eks_one_apoint` | `serialize_auditor_eks` with one **A_POINT** `CompressedPubkey` | none | **`vector<u8>`** length **32** | VM↔Lean (**`ldConst` 10**, index **114**). Corpus: [`serialize_auditor_eks_single_a_point.hex`](../corpora/confidential_assets/serialize_auditor_eks_single_a_point.hex). |
| `amounts_one_zero` | `serialize_auditor_amounts` with one **`new_pending_balance_no_randomness`** | none | **`vector<u8>`** length **256** (all **zero** on current VM) | VM↔Lean (**`ldConst` 11**, index **115**). Corpus: [`serialize_auditor_amounts_one_zero_pending.hex`](../corpora/confidential_assets/serialize_auditor_amounts_one_zero_pending.hex). |
| `eks_two_apoint` | `serialize_auditor_eks` with two **A_POINT** pubkeys | none | **`vector<u8>`** length **64** | VM↔Lean (**`ldConst` 12**, index **116**). Corpus: [`serialize_auditor_eks_two_a_points.hex`](../corpora/confidential_assets/serialize_auditor_eks_two_a_points.hex). |
| `eks_three_apoint` | `serialize_auditor_eks` with three **A_POINT** pubkeys | none | **`vector<u8>`** length **96** | VM↔Lean (**`ldConst` 20**, index **124**). Corpus: [`serialize_auditor_eks_three_a_points.hex`](../corpora/confidential_assets/serialize_auditor_eks_three_a_points.hex). |
| `eks_four_apoint` | `serialize_auditor_eks` with four **A_POINT** pubkeys | none | **`vector<u8>`** length **128** | VM↔Lean (**`ldConst` 21**, index **125**). Corpus: [`serialize_auditor_eks_four_a_points.hex`](../corpora/confidential_assets/serialize_auditor_eks_four_a_points.hex). |
| `eks_five_apoint` | `serialize_auditor_eks` with five **A_POINT** pubkeys | none | **`vector<u8>`** length **160** | VM↔Lean (**`ldConst` 22**, index **126**). Corpus: [`serialize_auditor_eks_five_a_points.hex`](../corpora/confidential_assets/serialize_auditor_eks_five_a_points.hex). |
| `amounts_two_zero` | `serialize_auditor_amounts` with two zero pending balances | none | **`vector<u8>`** length **512** (all **zero** on current VM) | VM↔Lean (**`ldConst` 13**, index **117**). Corpus: [`serialize_auditor_amounts_two_zero_pending.hex`](../corpora/confidential_assets/serialize_auditor_amounts_two_zero_pending.hex). |
| `amounts_one_u64_1` | `serialize_auditor_amounts` with **`new_pending_balance_u64_no_randonmess(1)`** | none | **`vector<u8>`** length **256** | VM↔Lean (**`ldConst` 14**, index **118**). Corpus + **literal** in `Programs/Confidential.lean` — **regenerate** if VM wire changes. |
| `amounts_one_actual_zero` | `serialize_auditor_amounts` with **`new_actual_balance_no_randomness`** | none | **`vector<u8>`** length **512** (all **zero** on current VM) | VM↔Lean (**`ldConst` 15**, index **119**). Corpus: [`serialize_auditor_amounts_one_actual_zero.hex`](../corpora/confidential_assets/serialize_auditor_amounts_one_actual_zero.hex). |
| `amounts_zero_then_u64_1` | `serialize_auditor_amounts` with zero pending then **`new_pending_balance_u64_no_randonmess(1)`** | none | **`vector<u8>`** length **512** | VM↔Lean (**`ldConst` 16**, index **120**). Corpus: [`serialize_auditor_amounts_zero_then_u64_one_pending.hex`](../corpora/confidential_assets/serialize_auditor_amounts_zero_then_u64_one_pending.hex) (= one-zero ‖ u64-one). |
| `amounts_u64_1_then_zero` | Same balances in **reverse** vector order (`u64(1)` then zero pending) | none | **`vector<u8>`** length **512** | VM↔Lean (**`ldConst` 17**, index **121**). Corpus: [`serialize_auditor_amounts_u64_one_then_zero_pending.hex`](../corpora/confidential_assets/serialize_auditor_amounts_u64_one_then_zero_pending.hex). Lean: `serializeAuditorAmounts_mixed512_orders_distinct`. |
| `amounts_actual_then_u64_1` | **`new_actual_balance_no_randomness`** then **`new_pending_balance_u64_no_randonmess(1)`** | none | **`vector<u8>`** length **768** | VM↔Lean (**`ldConst` 18**, index **122**). Corpus: [`serialize_auditor_amounts_actual_zero_then_u64_one_pending.hex`](../corpora/confidential_assets/serialize_auditor_amounts_actual_zero_then_u64_one_pending.hex). |
| `amounts_u64_1_then_actual` | **`u64(1)`** pending then actual zero (reverse of **122**) | none | **`vector<u8>`** length **768** | VM↔Lean (**`ldConst` 19**, index **123**). Corpus: [`serialize_auditor_amounts_u64_one_pending_then_actual_zero.hex`](../corpora/confidential_assets/serialize_auditor_amounts_u64_one_pending_then_actual_zero.hex). Lean: `serializeAuditorAmounts_mixed768_orders_distinct`. |
| `e2e_xfer_mismatch` | `confidential_transfer` | two valid **`pack_confidential_transfer_proof_simple`** bundles at same **`actual_balance`**, splice **`recipient_amount`** from wrong cleartext **`u64`** | **`MoveAbort`** **`65553`** (`EINVALID_SENDER_AMOUNT`) | **VM:** real entry + proof bytes. **Lean:** merged fragment witness **182** (`caE2eAbort65553Desc`); **`RunnerFuncMappingAux`**. |
| `e2e_xfer_recipient_frozen` | `confidential_transfer` | recipient **`freeze_token`** then sender **`confidential_transfer`** | **`MoveAbort`** **`196615`** (`EALREADY_FROZEN` / `invalid_state(7)`) | **VM:** real entry. **Lean:** witness **183** (`caE2eAbort196615Desc`); **`RunnerFuncMappingAux`**. |
| `e2e_xfer_empty_auditors` | `confidential_transfer` | asset auditor set; voluntary auditor list **empty** | **`MoveAbort`** **`65542`** (`EINVALID_AUDITORS` / `invalid_argument(6)`) | **VM:** **`validate_auditors`** fails before **`verify_transfer_proof`**. **Lean:** **42** (`caE2eAbort65542Desc`); **`RunnerFuncMappingAux`**. |
| `e2e_xfer_wrong_asset_auditor_pk` | `confidential_transfer` | asset auditor set; first voluntary EK ≠ asset auditor EK | **`65542`** | **VM:** same auditor gate. **Lean:** **42** (shared **65542** stub with empty-auditor row). |
| `e2e_deposit_to_recipient_frozen` | `deposit_to` | recipient **`freeze_token`** then cross-party **`deposit_to`** | **`196615`** | **VM:** real entry. **Lean:** **183** (same stub as frozen-recipient **`confidential_transfer`**). |
| `e2e_deposit_self_when_frozen` | **`deposit`** (self) | **`freeze_token`** then **`deposit`** to same account | **`196615`** | **VM:** **`deposit_to_internal`** rejects frozen **`to`** (sender = recipient). **Lean:** **183**. |
| `e2e_freeze_twice` | `freeze_token` | two **`freeze_token`** calls without **`unfreeze_token`** | **`196615`** | **VM:** second call aborts. **Lean:** **183**. |
| `e2e_unfreeze_not_frozen` | `unfreeze_token` | **`unfreeze_token`** right after **`register`** (never frozen) | **`196616`** (`ENOT_FROZEN` / `invalid_state(8)`) | **VM:** real entry. **Lean:** **185** (`caE2eAbort196616Desc`). |
| `e2e_register_twice` | **`register`** | successful **`register`** then repeat with same proofs | **`524290`** (`ECA_STORE_ALREADY_PUBLISHED` / `already_exists(2)`) | **VM:** real entry. **Lean:** **186** (`caE2eAbort524290Desc`). |
| `e2e_rollover_twice_denorm` | **`rollover_pending_balance`** | **`deposit`** then **`rollover_pending_balance`** twice without **`normalize`** | **`196618`** (`ENORMALIZATION_REQUIRED` / `invalid_state(10)`) | **VM:** second rollover gate. **Lean:** **187** (`caE2eAbort196618Desc`). |
| `e2e_enable_token_twice` | **`enable_token`** (bypass) | framework **`enable_token`** twice on same metadata | **`196620`** (`ETOKEN_ENABLED` / `invalid_state(12)`) | **VM:** **`try_exec_function_bypass_at`** (no `entry` wrapper). **Lean:** **188** (`caE2eAbort196620Desc`). |
| `e2e_deposit_token_disabled_allow_list` | **`deposit`** | **`register`** while allow list off, then **`enable_allow_list`**, then **`deposit`** (no **`FAConfig.allowed`** / missing **`FAConfig`**) | **`65549`** (`ETOKEN_DISABLED` / `invalid_argument(13)`) | **VM:** real entry. **Lean:** **189** (`caE2eAbort65549Desc`). |
| `e2e_enable_allow_list_twice` | **`enable_allow_list`** (bypass) | two **`enable_allow_list`** calls | **`196622`** (`EALLOW_LIST_ENABLED` / `invalid_state(14)`) | **VM:** bypass. **Lean:** **190** (`caE2eAbort196622Desc`). |
| `e2e_disable_allow_list_twice` | **`disable_allow_list`** (bypass) | **`enable_allow_list`** → **`disable_allow_list`** → second **`disable_allow_list`** | **`196623`** (`EALLOW_LIST_DISABLED` / `invalid_state(15)`) | **VM:** bypass. **Lean:** **191** (`caE2eAbort196623Desc`). |
| `e2e_register_allow_list_before_token` | **`register`** | **`enable_allow_list`** then first **`register`** (no prior **`enable_token`**) | **`65549`** (`invalid_argument(ETOKEN_DISABLED)`) | **VM:** real entry. **Lean:** **189** (shared **`65549`** stub with post-allow-list **`deposit`** rows). |
| `e2e_deposit_after_disable_token_allow_list` | **`deposit`** | **`register`** → **`enable_token`** → **`enable_allow_list`** → **`disable_token`** → **`deposit`** | **`65549`** | **VM:** **`is_token_allowed`** false. **Lean:** **189**. |
| `e2e_freeze_token_store_not_published` | **`freeze_token`** | account never **`register`** | **`393219`** (`not_found` / **`ECA_STORE_NOT_PUBLISHED`**) | **VM:** real entry. **Lean:** **192** (`caE2eAbort393219Desc`). |
| `e2e_unfreeze_token_store_not_published` | **`unfreeze_token`** | account never **`register`** | **`393219`** | **VM:** real entry. **Lean:** **192** (shared **`not_found`** stub). |
| `e2e_rollover_store_not_published` | **`rollover_pending_balance`** | account never **`register`** | **`393219`** | **VM:** real entry. **Lean:** **192**. |
| `e2e_rollover_and_freeze_store_not_published` | **`rollover_pending_balance_and_freeze`** | account never **`register`** (fails in nested **`rollover_pending_balance`**) | **`393219`** | **VM:** real entry. **Lean:** **192**. |
| `e2e_disable_token_twice` | **`disable_token`** (bypass) | **`register`** → **`enable_token`** → **`enable_allow_list`** → two **`disable_token`** | **`196621`** (`invalid_state(ETOKEN_DISABLED)` on second call) | **VM:** bypass. **Lean:** **193** (`caE2eAbort196621Desc`). |
| `e2e_norm_twice` | `normalize` | **`deposit`** + **`rollover`** → **`normalize`** succeeds → second **`normalize`** with same packed args | **`MoveAbort`** **`196619`** (`EALREADY_NORMALIZED` / `invalid_state(11)`) | **VM:** abort before second **`verify_normalization_proof`**. **Lean:** witness **184** (`caE2eAbort196619Desc`); **`RunnerFuncMappingAux`**. |
| *(stretch)* entrypoints / `*_internal` with store+FA | `confidential_asset` | real signers + published `ConfidentialAssetStore` + FA | abort / state | **VM:** covered by **`e2e-move-tests`** (`confidential_asset_e2e.rs`, plan **§7.0**). **`move-lean-difftest`:** still Option B / **Blocked** for VM↔Lean (**§7.1**). |
| *(stretch)* `verify_*` + registration | `confidential_proof` | valid proof structs / friend-visible registration path | abort / unit | **VM:** same **e2e** harness (real `register` → `verify_registration_proof`; transfers/withdraw/rotate). **`move-lean-difftest` + Lean:** narrow smoke includes **171** (production **`verify_registration_proof_for_difftest`** on fixed fixture; friend-only **`verify_registration_proof`** on `register` still **e2e**). |
| *(stretch)* bytecode parity | Lean `MoveModel.*` | disasm → `MoveInstr` + natives per opcode | same as VM | **Blocked** — program-sized (**§7.3**). |

## 8.1 Function-by-function coverage matrix (as of 2026-04-17)

Status legend:

- **✓** — direct VM↔Lean difftest row exists (may be multiple).
- **e2e** — covered only through the merged CA end-to-end oracle (abort-path only; happy path needs live crypto on-chain).
- **BLOCKED(harness)** — a native (currently `ristretto255::point_clone`) is not
  registered in `move-vm-test-utils`; the VM aborts with
  `E_NATIVE_FUN_NOT_AVAILABLE` (canonical abort `196613`) so no real-semantics
  oracle can be produced.
- **BLOCKED(Lean)** — the Lean evaluator has no model for the required crypto
  primitive (`scalar_mul`, `point_mul`, `point_add`, `new_scalar_from_sha2_512`,
  Bulletproofs `verify_batch_range_proof`, sigma-protocol transcript);
  pinning the VM would compare against a placeholder instead of real
  semantics.
- **`#[test_only]`** — not callable from the harness module (`0x1::difftest_*`);
  promoting to `public fun ..._for_difftest` unblocks (see
  `prove_registration_deterministic_for_difftest` precedent).
- **friend-only** — `public(friend)` restricts callers to
  `aptos_experimental::confidential_asset`; unreachable from the harness.
- **storage-dependent** — function signature consumes `&signer` /
  `borrow_global` / `Object<Metadata>`; cannot be driven from the harness
  module which has no FA/Object state. Covered indirectly via e2e oracle
  aborts.

### `ristretto255_twisted_elgamal.move` — 20/20 production-visible, 0 blocked (`ciphertext_clone` unblocked by Phase E bulletproofs-feature harness fix)

| Function | Status | Example covering row |
|---|---|---|
| `new_pubkey_from_bytes` | ✓ | `test_elg_pubkey_roundtrip_bytes_ok`, `..._invalid_len_is_none` |
| `pubkey_to_bytes` | ✓ | `test_elg_pubkey_to_bytes_len_is_32`, roundtrip rows |
| `pubkey_to_point` | ✓ | via `test_elg_pubkey_to_point_roundtrips` |
| `pubkey_to_compressed_point` | ✓ | indirect via `pubkey_to_bytes` chain (`compressed_point_to_bytes` ∘ `pubkey_to_compressed_point` = `pubkey_to_bytes`) |
| `new_ciphertext_from_bytes` | ✓ | 5 dedicated rows (valid, invalid, zero-canonical, roundtrip, length-reject) |
| `new_ciphertext_no_randomness` | ✓ | every `ct(n)` row uses it |
| `ciphertext_from_points` | ✓ | `test_elg_ciphertext_from_points_distinguishes_left_right` |
| `ciphertext_from_compressed_points` | ✓ | `test_elg_from_compressed_points_preserves_order`, `..._roundtrip` |
| `ciphertext_to_bytes` | ✓ | 4 byte-layout pins + roundtrip rows |
| `ciphertext_into_points` | ✓ | `test_elg_get_value_component_matches_into_points_left_nonzero` |
| `ciphertext_as_points` | ✓ | `test_elg_ciphertext_as_points_compress_equals_to_bytes` |
| `compress_ciphertext` / `decompress_ciphertext` | ✓ | 3 dedicated rows incl. `0xffff` edge |
| `ciphertext_add` / `ciphertext_add_assign` | ✓ | multiple non-zero algebra rows |
| `ciphertext_sub` / `ciphertext_sub_assign` | ✓ | multiple non-zero algebra rows + non-commutativity pin |
| `ciphertext_equals` | ✓ | reflexive + commutative pins, non-zero discriminator |
| `get_value_component` | ✓ | `..._matches_into_points_left_nonzero`, `..._not_identity_when_v_nonzero` |
| `ciphertext_clone` | ✓ | `test_elg_ciphertext_clone_equals_original_nonzero`, `..._bytes_identical_nonzero`, `..._is_structurally_independent`, `..._zero_encodes_all_zero` (Phase E — `point_clone` harness unblock) |
| `pubkey_from_secret_key` | `#[test_only]` | — |
| `new_ciphertext` | `#[test_only]` | — |
| `new_ciphertext_with_basepoint` | `#[test_only]` | — |
| `generate_twisted_elgamal_keypair` | `#[test_only]` | — |

### `confidential_balance.move` — 22/22 production-visible, 0 blocked (`balance_to_points_{c,d}` unblocked by Phase E bulletproofs-feature harness fix)

| Function | Status | Example covering row |
|---|---|---|
| `new_pending_balance_no_randomness` | ✓ | used by every balance row |
| `new_actual_balance_no_randomness` | ✓ | `test_bal_zero_actual_bytes_all_zero`, e2e rows |
| `new_compressed_pending_balance_no_randomness` | ✓ | `test_bal_compressed_pending_no_rand_matches_plain` |
| `new_compressed_actual_balance_no_randomness` | ✓ | `test_bal_compressed_actual_no_rand_matches_plain` |
| `new_pending_balance_u64_no_randonmess` | ✓ | every `pending_u64(n)` row |
| `new_pending_balance_from_bytes` | ✓ | 5 rows: valid roundtrip, invalid chunks 0 / 3, 256-zeros, 255/257-len |
| `new_actual_balance_from_bytes` | ✓ | 512-zero decode + roundtrip |
| `compress_balance` / `decompress_balance` | ✓ | byte-roundtrip `pending_u64(7)` + zero variants |
| `balance_to_bytes` | ✓ | 4 byte-layout pins (256/512 zeros + `pending_u64(1)` first 32 B) |
| `balance_to_points_c` | ✓ | `test_bal_balance_to_points_c_*` (11 rows: zero pending/actual length pins, non-zero first-point structural pins — Phase E unblock) |
| `balance_to_points_d` | ✓ | `test_bal_balance_to_points_d_*` (11 rows: zero pending/actual length pins, non-zero first-point structural pins — Phase E unblock; transitively used by `fiat_shamir_transfer_sigma_proof_challenge`) |
| `add_balances_mut` | ✓ | 3-way accumulator + commutativity + zero-identity pins |
| `sub_balances_mut` | ✓ | **5 rows incl. the Bug #1 catch** + chain + non-commutativity |
| `balance_equals` | ✓ | 3 rows incl. commutativity on distinct |
| `balance_c_equals` | ✓ | C-only discriminator + distinct-u64 pin |
| `is_zero_balance` | ✓ | chunk-0/1/2/3 strict pins |
| `split_into_chunks_u64` | ✓ | 4 rows incl. `u64::MAX` all-chunks-`0xffff` |
| `split_into_chunks_u128` | ✓ | 3 rows incl. `0xffff << 112` top-chunk pin |
| `get_pending_balance_chunks` / `get_actual_balance_chunks` / `get_chunk_size_bits` | ✓ | dedicated rows + structural layer reexport pins |
| `generate_balance_randomness` | `#[test_only]` | — |
| `balance_randomness_as_scalars` | `#[test_only]` | — |
| `new_actual_balance_from_u128` | `#[test_only]` | — |
| `new_pending_balance_from_u64` | `#[test_only]` | — |
| `verify_actual_balance` | `#[test_only]` | — |
| `verify_pending_balance` | `#[test_only]` | — |

### `confidential_proof.move` — 11/21 production-visible directly; 4 BLOCKED(Lean); 4 `#[test_only]`-gated deserializers; 2 friend-only

| Function | Status | Notes |
|---|---|---|
| `verify_registration_proof` (friend) | ✓ | covered via `_for_difftest` wrapper; deterministic prover round-trip pinned |
| `registration_fs_message_for_test` | ✓ | byte-exact goldens |
| `prove_registration_deterministic_for_difftest` | ✓ | used by the round-trip row |
| `verify_registration_proof_for_difftest` | ✓ | public wrapper |
| `verify_withdrawal_proof` | **negative-pin only (Lean witness **195**)** | happy-path still needs `ristretto255::{scalar_mul, point_mul, point_add}` + `new_scalar_from_sha2_512` + `bulletproofs::verify_batch_range_proof` modeled as natives (Phase D.2/D.3). Negative direction covered by `test_verify_withdrawal_proof_zero_sigma_aborts` (Phase D.1): feeds length-valid all-zero sigma bytes through `deserialize_withdrawal_proof` → `verify_withdrawal_proof` and pins the canonical `ESIGMA_PROTOCOL_VERIFY_FAILED` (**65537**) abort. |
| `verify_transfer_proof` | **negative-pin only (Lean witness **195**)** | same as above, plus auditor-MSM; Phase D.1 covers `test_verify_transfer_proof_zero_sigma_aborts` — all-zero 1792 B sigma + empty auditor quads + empty range proof through the real `verify_transfer_proof`, pinning abort **65537**. |
| `verify_normalization_proof` | **negative-pin only (Lean witness **195**)** | same; Phase D.1 covers `test_verify_normalization_proof_zero_sigma_aborts` — all-zero 1152 B sigma + empty range proof, pinning abort **65537**. |
| `verify_rotation_proof` | **negative-pin only (Lean witness **195**)** | same; Phase D.1 covers `test_verify_rotation_proof_zero_sigma_aborts` — all-zero 1184 B sigma + empty range proof, pinning abort **65537**. |
| `deserialize_withdrawal_proof` | **gated on `#[test_only]` promotion (Phase C)** | test vectors only producible via `prove_withdrawal` which is `#[test_only]`; Phase C promotes `prove_*` to `public fun prove_*_deterministic_for_difftest` following the registration precedent |
| `deserialize_transfer_proof` | **gated on `#[test_only]` promotion (Phase C)** | same |
| `deserialize_normalization_proof` | **gated on `#[test_only]` promotion (Phase C)** | same |
| `deserialize_rotation_proof` | **gated on `#[test_only]` promotion (Phase C)** | same |
| `auditors_count_in_transfer_proof` (friend) | friend-only | reachable only from `confidential_asset`; indirectly exercised by e2e transfer rows |
| `transfer_proof_ek_volun_auds_flat_bytes` (friend) | friend-only | same |
| `get_fiat_shamir_withdrawal_sigma_dst` | ✓ | exact-bytes + pairwise-inequality pins |
| `get_fiat_shamir_transfer_sigma_dst` | ✓ | same |
| `get_fiat_shamir_normalization_sigma_dst` | ✓ | same |
| `get_fiat_shamir_rotation_sigma_dst` | ✓ | same |
| `get_fiat_shamir_registration_sigma_dst` | ✓ | same |
| `get_bulletproofs_dst` | ✓ | exact-bytes pin + not-equal-any-sigma-DST pin |
| `get_bulletproofs_num_bits` | ✓ | `== 16` pin |
| `prove_withdrawal`, `prove_transfer`, `prove_normalization`, `prove_rotation`, `prove_registration` | `#[test_only]` | targeted by Phase C promotion |
| `serialize_{withdrawal,transfer,normalization,rotation}_proof` | `#[test_only]` | targeted by Phase C |
| `verify_registration_proof_for_test` | `#[test_only]` | superseded by `_for_difftest` |
| `deserialize_*_sigma_proof`, `fiat_shamir_*_challenge`, `msm_*_gammas`, `msm_gamma_{1,2}`, `scalar_mul_3`, `scalar_linear_combination`, `new_scalar_from_pow2`, `prepend_domain_context`, `prove_new_balance_range`, `prove_transfer_amount_range`, `verify_*_sigma_proof`, `verify_*_range_proof`, `msm_transfer_gammas` | `fun` (private) | internal helpers; exercised transitively when Phase C lands proofs |

### `confidential_asset.move` — direct-difftestable helpers only

| Function | Status | Notes |
|---|---|---|
| `max_sender_auditor_hint_bytes` (#[view]) | ✓ | pure constant; pinned `== 256` |
| `serialize_auditor_eks` (public) | ✓ | 7 rows: empty, 1, 2, 3, 4, 5, 6 `A_POINT` pubkeys + order-matters pin |
| `serialize_auditor_amounts` (public) | ✓ | 8 rows incl. distinct-amount discriminator + order-matters |
| `has_confidential_asset_store`, `is_token_allowed`, `is_allow_list_enabled`, `pending_balance`, `actual_balance`, `encryption_key`, `is_normalized`, `is_frozen`, `get_auditor`, `confidential_asset_balance` (all #[view]) | storage-dependent | need live chain state; covered transitively by merged e2e oracle on abort paths |
| `enable_allow_list`, `disable_allow_list`, `enable_token`, `disable_token`, `set_auditor` | storage-dependent | admin wrappers; covered by e2e abort rows |
| `register_internal`, `deposit_to_internal`, `withdraw_to_internal`, `confidential_transfer_internal`, `rotate_encryption_key_internal`, `normalize_internal`, `rollover_pending_balance_internal`, `freeze_token_internal`, `unfreeze_token_internal` | e2e (abort) | all 9 happy paths require live on-chain crypto + valid proofs. Abort branches are pinned via the 168-row merged e2e oracle (`DIFTEST_MERGE_CA_E2E=1`) |
| `init_module`, `get_fa_store_{signer,address}`, `get_fa_config_{signer,address}`, `get_user_{signer,address}`, `construct_user_seed`, `construct_fa_seed`, `validate_auditors`, `deserialize_auditor_eks`, `deserialize_auditor_amounts`, `ensure_sufficient_fa`, `ensure_fa_config_exists` | storage-dependent or private | — |
| `init_module_for_testing`, `register_for_testing`, `verify_{pending,actual}_balance`, `assert_last_*_event*` | `#[test_only]` | — |

### `confidential_gas_e2e_helpers.move` — 0/5 directly difftested

| Function | Status | Notes |
|---|---|---|
| `pack_withdraw_to_proof`, `pack_confidential_transfer_proof_simple`, `pack_confidential_transfer_proof_with_auditors`, `pack_rotate_encryption_key_proof`, `pack_normalization_proof` | e2e | pure data-packing helpers consumed by the gas-estimator flow. Exercised transitively by e2e rows that land gas-estimation transactions but no dedicated unit-level difftest row exists. Low regression surface (straight struct constructions). |

### Coverage totals (production-visible, excluding `#[test_only]` / friend-only)

| Module | Directly covered | BLOCKED(harness) | BLOCKED(Lean) | Gated on Phase C promotion | storage-dependent / e2e only |
|---|---|---|---|---|---|
| `ristretto255_twisted_elgamal` | 20 | 0 | 0 | 0 | 0 |
| `confidential_balance` | 22 | 0 | 0 | 0 | 0 |
| `confidential_proof` | 11 | 0 | 4 | 4 | 0 (friend-only: 2) |
| `confidential_asset` | 3 | 0 | 0 | 0 | 19+ |
| `confidential_gas_e2e_helpers` | 0 | 0 | 0 | 0 | 5 |

### What would unblock each bucket

| Bucket | Unblocker |
|---|---|
| BLOCKED(harness) (0 rows) | All previously `BLOCKED(harness)` rows (`ciphertext_clone`, `balance_to_points_c`, `balance_to_points_d`) were unblocked in Phase E by registering the `BULLETPROOFS_NATIVES` (24) and `BULLETPROOFS_BATCH_NATIVES` (87) feature bits in the harness `Features` resource — see Harness-Infra #1 in `inventory/bug_fixes_found_by_difftests.md`. |
| BLOCKED(Lean) (4 `verify_*_proof`) | Model `ristretto255::{scalar_mul, point_mul, point_add, point_sub, new_scalar_from_sha2_512}` **and** `ristretto255_bulletproofs::verify_batch_range_proof` as Lean natives. Substantial — essentially implementing a Bulletproofs + sigma-protocol verifier in Lean (or axiomatizing with a trusted native oracle). |
| Gated on Phase C (4 `deserialize_*_proof`) | Promote `#[test_only]` `prove_{withdrawal,transfer,normalization,rotation}` + their `*SigmaProofRandomness` structs to `public fun prove_*_deterministic_for_difftest` — same precedent as `prove_registration_deterministic_for_difftest`. Follow-up this phase, see §10 changelog entry. |
| storage-dependent / e2e only (~24 functions in `confidential_asset`) | Either (i) build an in-module diff-test state shim (synthetic `FAController` / `ConfidentialAssetStore`) so `*_internal` can be called against local storage, or (ii) rely on the merged e2e oracle (already covers abort paths). Happy paths additionally require live on-chain Ristretto/Bulletproofs — tied to the BLOCKED(Lean) bucket. |

## 9. VM-only vs VM↔Lean (decision log)

| Situation | Choice |
| --------- | ------ |
| Lean `MoveInstr` / `Step` / native missing | **VM-only** oracle + **Blocked** row until Lean catches up **or** narrow case. |
| Pure `u64` / `vector<u8>` / bool return, no globals | **VM↔Lean** once `ModuleEnv` + runner wired. |
| Entry touching `borrow_global` / FA | **Blocked** for full `MoveModel.*` until L4-style store (see formal verification plan) **or** test `*_internal` with synthetic locals only. |

## 10. Changelog

| Date | Change |
| ---- | ------ |
| 2026-04-17 | **Phase U — pairwise-swap / reorder FS prefix goldens (2 rows, 0 latent bugs; previously-undetectable "symmetric-fixture swap invisible" regression class now covered).** Phases R, S, and T together pinned FS prefix bytes for 0-auditor (R), 1-auditor (S), and 2-auditor (T) transfer, amount=42 (R) and amount=u64::MAX (T) withdrawal, and zero-zero (R) and current-zero-vs-zero-new (T) normalization / rotation — but each fixture still leaves a specific SWAP class invisible: R's amount=42 makes chunks 1-3 all zero so swapping any two of chunks 1/2/3 is a byte-level no-op; T's amount=u64::MAX makes every chunk identical so swapping chunks i↔j is also a byte-level no-op; R's zero-zero rotation gives `balance_to_bytes(zero) == balance_to_bytes(zero)` so current↔new concat swap is invisible. **Pin strategy:** two complementary "distinct-value" fixtures. (a) Withdrawal with `amount = 0x0004_0003_0002_0001` ⇒ chunks = [1, 2, 3, 4] pairwise-distinct 16-bit values: any pairwise chunk swap in the amount-chunk iteration flips exactly two 32-byte scalar blobs. (b) Rotation with two DISTINCT non-zero balances: `current_balance` has C[0] = basepoint (reuses `nonzero_actual_bal_for_fs_tests`), `new_balance` has D[0] = basepoint (new helper `nonzero_actual_bal_d_for_fs_tests`). A bug that (i) swaps `balance_to_bytes(current) ↔ balance_to_bytes(new)` call order, (ii) concats them in reversed order, or (iii) serializes both from the same source flips the pinned bytes. Added two rows: `test_fs_prefix_wd_distinct_chunks_matches_golden` (837 B) + `test_fs_prefix_rot_nonzero_both_matches_golden` (1251 B). Each maps to Lean `funcIdx := 40` (`ldTrue`). **Runs (post-addition):** `./difftest.sh --suite confidential` → **459 passed / 0 failed** (up from 457). The transfer / withdrawal / normalization / rotation FS prefix surface is now pinned byte-for-byte across SIX fixture axes total: (i) 0-auditor (R), (ii) 1-auditor (S), (iii) 2-auditor (T.tr), (iv) zero-balance (R), (v) one-side-nonzero (T.norm), (vi) both-sides-nonzero-distinct (U.rot); and across THREE amount-chunk axes: (a) single-chunk (R, amount=42), (b) uniform-chunk (T, amount=u64::MAX), (c) pairwise-distinct chunks (U, amount=0x4_0003_0002_0001). |
| 2026-04-17 | **Phase T — boundary / multi-auditor / non-zero-balance FS prefix goldens (3 rows, 0 latent bugs; previously-undetectable "second-iteration bug / high-chunk bug / D-component swap bug" regression classes now covered).** Phase R/S goldens pin bytes for the "base" fixture (withdrawal amount=42 with chunks 1-3 all zero; transfer with 0 and 1 auditor; zero balances everywhere). Three regression classes remain NOT observable under those fixtures: (i) a bug in `split_into_chunks_u64` that mis-shifts high chunks, or a refactor that walks `amount_chunks.for_each_ref(|chunk| bytes.append(scalar_to_bytes(chunk)))` but terminates at the wrong length, only manifests when chunks 1-3 are non-zero; (ii) a bug that silently skips the SECOND iteration of the auditor-loop body (or off-by-one that iterates `auditor_eks[0..len-1]`) passes Phase S's 1-auditor row because the single iteration is fine, but fails on 2 auditors; (iii) a bug in the per-chunk C-vs-D point extraction in `balance_to_bytes`, or a chunk-concat bug (e.g., reversed order), is invisible under zero balances (all chunks = 64 zero bytes) but manifests when exactly one chunk has a distinctive bit pattern. **Pin strategy:** three new fixtures — (a) withdrawal with `amount = u64::MAX` ⇒ chunks = [0xffff, 0xffff, 0xffff, 0xffff] (837 B, exercises full 4-chunk amount path); (b) transfer with 2 DISTINCT auditor eks (hash_base + basepoint) and 2 auditor amounts (zero + chunk-0-D = basepoint) (1955 B, exercises 2nd-iteration auditor-loop body); (c) normalization with non-zero current balance (C[0] = basepoint) and zero new balance (1224 B, exercises per-chunk C-point extraction). Each extracted from the Move VM oracle and pinned byte-for-byte. Added three rows: `test_fs_prefix_wd_u64max_matches_golden`, `test_fs_prefix_tr_2aud_matches_golden`, `test_fs_prefix_norm_nonzero_matches_golden`. Each maps to Lean `funcIdx := 40` (`ldTrue`). **Runs (post-addition):** `./difftest.sh --suite confidential` → **457 passed / 0 failed** (up from 454). Together with Phases R, S, and U, the FS prefix surface is pinned across distinct structural axes (see Phase U for full enumeration). |
| 2026-04-17 | **Phase S — transfer FS prefix second-fixture golden with 1 auditor (1 row, 0 latent bugs; previously-undetectable "auditor-vector iteration silently skipped" regression class now covered).** Phase R's transfer golden uses 0 auditors — the `auditor_eks.for_each_ref(|ek| ...)` and `auditor_amounts.for_each_ref(|bal| ...)` loops never execute a body and are only pinned at "length 0". A regression that silently skips the auditor loop on non-empty vectors (e.g. a refactor that uses `.for_each` but returns early on len==0, or an off-by-one that hashes only `auditor_eks[0..len-1]`, or inadvertently swaps `for_each_ref` for `for_each` on a reference that evaporates) would still pass every Phase R / G / G.2 / N row. **Pin strategy:** identical fixture to the Phase R transfer golden (chain_id=9, sender=@0xA, contract=@0xB, sender_ek = basepoint, recipient_ek = hash_to_point_base, zero-balance pending/actual) but with exactly ONE auditor ek (= hash_to_point_base) and ONE auditor amount (= zero pending balance, 128 B). The golden prefix is **1795 B** = 1635 (Phase R 0-auditor base) + 32 (auditor ek) + 128 (auditor amount). A single-bit drift in the auditor-loop body — skipping, truncating, reordering, or any per-iteration byte layout change — flips the prefix and fails the row. Added one row: `test_fs_prefix_tr_1aud_matches_golden` mapped to Lean `funcIdx := 40` (`ldTrue`). **Runs (post-addition):** `./difftest.sh --suite confidential` → **454 passed / 0 failed** (up from 453). No production bug surfaced, but the auditor-loop-body regression class — orthogonal to Phase R (structural 0-auditor base) and to `auditor_count_matters` / `auditor_*_content_matters` (Phase N/G.2 structural (in)equalities) — is now pinned bit-for-bit. |
| 2026-04-17 | **Phase R.1 — golden-vector byte pin for `registration_fs_message_for_test` (1 row, 0 latent bugs; previously-undetectable "symmetric drift between production helper and test helper" regression class now covered).** Before Phase R.1, the existing row `test_registration_fs_message_framework_matches_helpers_golden` pinned the production helper against `difftest_registration_helpers::registration_fs_message_golden_move`. But that Move helper LIVE-RECONSTRUCTS the message from the same primitives (`FIAT_SHAMIR_REGISTRATION_SIGMA_DST`, `bcs::to_bytes`, `pubkey_to_bytes`, `compressed_point_to_bytes`) that the production code uses — so a symmetric byte-layout drift in any of those primitives would shift BOTH sides equally and the row would still pass. The Phase R.1 pin is a HEX CONSTANT extracted from the Move VM oracle on 2026-04-17 for the same fixture (chain_id=9, sender=@0x1, contract=@0x2, token=@0x3, ek=basepoint): 199 B = DST(38) + chain_id(1) + sender(32) + contract(32) + token(32) + pubkey(32) + ek_bytes(32). Any single-bit drift in DST bytes, `bcs::to_bytes`, `pubkey_to_bytes`, or `compressed_point_to_bytes` flips the hex and fails the row. Added one row: `test_fs_reg_msg_matches_golden` mapped to Lean `funcIdx := 40` (`ldTrue`). **Runs (post-addition):** `./difftest.sh --suite confidential` → **453 passed / 0 failed** (up from 452). Together with Phase R (four sigma prefix goldens) and Phase Q (registration prover commitment/response goldens), the full FS-transcript byte-layout surface is now bit-for-bit pinned for all five CA proof systems. |
| 2026-04-17 | **Phase R — golden-vector byte pins for the four sigma FS prefix helpers (4 rows, 0 latent bugs; previously-undetectable "symmetric byte-layout drift across every FS prefix that preserves every Phase G / G.2 / N (in)equality" regression class now covered).** Phases G / G.2 / N pinned "starts with DST", "deterministic under identical inputs", "swap-matters" and individual-field "matters" pins for each of the four sigma FS prefixes (`withdrawal_fs_prefix_for_test`, `normalization_fs_prefix_for_test`, `rotation_fs_prefix_for_test`, `transfer_fs_prefix_for_test`). But every such row is a STRUCTURAL comparison of two prefix outputs — it only fires if the SIGN of the (in)equality flips. A subtle byte-layout drift that shifts EVERY prefix output symmetrically — e.g. a bug in `compressed_point_to_bytes` changing endian, a refactor of `prepend_domain_context` that swaps `chain_id` / `sender` / `contract_address` in a way that still produces a distinct byte under every Phase G swap, a subtle change in `balance_to_bytes` chunk-order, a `bcs::to_bytes` length-prefix regression — could leave Phase G / G.2 / N entirely intact while silently changing every prefix's byte layout. In production, an off-chain prover that hashes the pre-drift layout against an on-chain verifier that hashes the post-drift layout would produce disagreeing challenges and every transfer / withdrawal / normalization / rotation would fail. **Pin strategy** (dual of Phase Q but for the verifier's FS prefix): on a fixed, simple fixture — `chain_id=9, sender=@0xA, contract=@0xB`, `ek_g = basepoint_ek_for_fs_tests()` (basepoint as pubkey), `ek_h = hash_base_ek_for_fs_tests()` (hash_to_point_base as pubkey), zero-balance pending/actual with no randomness, withdrawal `amount = 42`, `0` auditors for transfer — pin each prefix helper's output to a byte-for-byte golden extracted from the Move VM oracle on 2026-04-17: withdrawal prefix = **837 B** (`4d6f76656d65...0000`), normalization prefix = **1224 B** (`4d6f76656d65...0000`), rotation prefix = **1251 B** (`4d6f76656d65...0000`), transfer prefix = **1635 B** (`4d6f76656d65...0000`). Each single-bit drift in any transitively-called primitive (`basepoint_compressed`, `compressed_point_to_bytes`, `hash_to_point_base`, `point_compress`, `pubkey_to_bytes`, `scalar_to_bytes`, `balance_to_bytes`, `prepend_domain_context`, `bcs::to_bytes`, the DST bytes themselves) flips the corresponding prefix and fails the row. Added four rows: `test_fs_prefix_{wd,norm,rot,tr}_matches_golden`. Each maps to Lean `funcIdx := 40` (`ldTrue`) for uniform bool column. **Production workflow:** identical to Phase Q — if one of these four rows fails, EITHER the FS-transcript algebra drifted (bug — investigate!) OR a deliberate production fix landed (regenerate the golden and log it in this changelog, per § 1 regression discipline). **Runs (post-addition):** `./difftest.sh --suite confidential` → **452 passed / 0 failed** (up from 448). Cumulative confidential total: **452 passed / 0 failed**. No production bug surfaced, but the entire "symmetric byte-layout drift across every FS prefix" regression class — orthogonal to Phase G / G.2 / N (structural (in)equality) — is now pinned bit-for-bit. |
| 2026-04-17 | **Phase Q — golden-vector byte pins for `prove_registration_deterministic_for_difftest` (2 rows, 0 latent bugs; previously-undetectable "symmetric algebraic drift that preserves every Phase O (in)equality" regression class now covered).** Phase O pinned algebraic (IN)EQUALITY relationships between different prover inputs — commitment invariant under non-`k` inputs, response variant under every FS-transcript input + `dk` + `k`. But every Phase O row is a *structural* comparison of two prover outputs; it fires only if the SIGN of the (in)equality flips. A subtle drift that shifts both outputs symmetrically — e.g. a bug in `new_scalar_from_sha2_512` that compresses bytes differently, a refactor of `point_compress` that swaps endian, or a non-algebra bug in `scalar_mul` / `scalar_invert` that affects *every* transitive call equally — could leave every Phase O row intact while silently changing the prover's output bytes. A golden-vector pin — "on this exact fixture, the prover MUST emit THESE exact bytes" — is strictly stronger: it binds the full algebraic transitive closure under the prover to a stable bit-for-bit output, firing on a single-bit change in any transitively-called primitive (`scalar_mul`, `scalar_sub`, `scalar_invert`, `new_scalar_from_sha2_512`, `basepoint_mul`, `point_mul`, `point_compress`, any byte-packing helper). **Pin strategy:** at the Phase O fixture `(chain_id=9, sender=@0xA, contract=@0xB, token=@0xC, dk=scalar(42), ek=pk_from_scalar(42), k=scalar(9999))`, pin `commitment_bytes == x"b268253d22a9668268094e11b365efe05314b3bf55e8f681d0a76124c52b1774"` and `response_bytes == x"8c6d7437c7e2f158cdd0da5d9202a7ecfbf15efcfa3176c50c925306b00a480d"` — both extracted from the Move VM oracle on 2026-04-17. Each maps to Lean `funcIdx := 40` (`ldTrue`) for uniform bool column. **Production workflow:** if this row fails, EITHER the prover's algebra drifted (bug — investigate!) OR a deliberate production fix landed (regenerate the golden and log it in this changelog, per § 1 regression discipline). Added two rows: `test_prove_reg_det_{commitment,response}_matches_golden`. **Runs (post-addition):** `./difftest.sh --suite confidential_proof` → **205 passed / 0 failed** (up from 203). Cumulative confidential total: **448 passed / 0 failed** (up from 446). |
| 2026-04-17 | **Phase P — verifier-side input-byte rejection pins for `verify_registration_proof_for_difftest` (6 rows, 0 latent bugs; previously-undetectable "wrong-length / non-canonical commitment or response silently accepted" regression class now covered).** Phase H pinned *semantic* mutation of the commitment / response (XOR one byte of an otherwise valid-canonical-length proof), which exercises the MSM equality check and aborts iff the check is intact. But two sibling regression classes are NOT caught by Phase H: (i) the length guards inside `new_compressed_point_from_bytes` / `new_scalar_from_bytes` — a refactor that drops the `vector::length == 32` precondition would silently accept 31- or 33-byte inputs and propagate garbage into the decode, producing a non-canonical point / scalar that still lands in the MSM; (ii) the canonicality filters — `point_is_canonical_internal` (rejects high-bit-set encodings) and `scalar_is_canonical_internal` (rejects `≥ L`) — a refactor that elides either check for performance opens a direct binding break: an attacker with any valid proof can produce infinitely many distinct-but-equivalent commitment / response byte strings, which then replay across sessions / chains / users. Phase H's XOR-one-byte mutation produces a *canonical* 32-byte encoding (almost always) that still parses; it cannot trigger the pre-parse rejection paths. **Pin strategy:** at a known-valid prover fixture (`dk=42, k=9999`), substitute ONE of the two sigma bytes-fields with (a) 31 zero bytes (too short), (b) 33 zero bytes (too long), or (c) 32 bytes of `0xff` — a pattern that violates BOTH canonicality predicates on both scalar- and point-sides (high bit set for points, `2^256 − 1 > L` for scalars). Require `verify_registration_proof_for_difftest` to abort with `error::invalid_argument(ESIGMA_PROTOCOL_VERIFY_FAILED) = 65537`. Added six rows: `test_verify_registration_rejects_{commitment,response}_{len_31,len_33,noncanonical_ff32}`. Reuses helpers `make_zero_bytes(n)` and `make_zero_bytes_with_ff_at(n, off)`. Each maps to Lean `caSigmaVerifyFailedAbortDesc` at `funcIdx := 195`. **Runs (post-addition):** `./difftest.sh --suite confidential_proof` → **203 passed / 0 failed** (up from 197); `--suite confidential_balance --suite confidential_elgamal` → **219 passed / 0 failed** (unchanged). Cumulative confidential total: **422 passed / 0 failed** (up from 416). No production bug surfaced, but the entire "verifier pre-parse path silently accepts malformed bytes" regression class — orthogonal to Phase H (MSM check) and to Phase O (prover transcript drift) — is now pinned. |
| 2026-04-17 | **Phase O — prover-side field-coverage pins for `prove_registration_deterministic_for_difftest` (17 rows, 0 latent bugs; previously-undetectable "prover silently drops input from its own transcript" regression class now covered).** Phases G / G.2 / N pin the FS *prefix* construction (the verifier's copy, wrapped by the public `*_fs_prefix_for_test` helpers), and Phase H pins the verifier's rejection logic when a single argument is mutated on the verify side. Neither covers the PROVER side — a regression that drops a field from the prover's transcript but not the verifier's would (i) still pass Phase H (the test rounds prove→verify in the same process, so both sides drop the field identically and agree on the wrong challenge), and (ii) still pass Phase G (Phase G pins the verifier's FS-prefix helper directly, not the prover's copy). In production, an off-chain prover emitting such a dropped-field proof would fail on-chain verification; worse, a prover that silently folded a non-`k` input into the commitment (e.g. `commitment = k*H + ek`) would leak binding information into a public value that should be input-invariant, breaking proof malleability protections. **Pin strategy:** exploit the deterministic prover's exact algebra: `commitment = point_compress(k * H)` depends ONLY on `k`; `response = k − e(·) · dk⁻¹` depends on every FS-transcript input (`chain_id`, `sender`, `contract_address`, `token_address`, `ek`, `commitment`) AND on `dk`. From this: (I) commitment MUST be invariant under change of every non-k input (6 rows: `test_prove_reg_det_commitment_invariant_under_{chain_id,sender,contract,token,ek,dk}`); (II) commitment MUST change when `k` changes (1 row: `test_prove_reg_det_commitment_changes_with_k`); (III) response MUST change under every FS-transcript input AND under `dk` AND under `k` (7 rows: `test_prove_reg_det_response_changes_with_{chain_id,sender,contract,token,ek,dk,k}`). Plus 3 sanity pins: commitment/response canonical 32-byte length (2 rows) and determinism under identical inputs (1 row). **Implementation:** each row runs `prove_registration_deterministic_for_difftest` twice — holding all arguments fixed except one, varying that one, and asserting the prescribed (in)equality on commitment / response bytes. Added helper `reg_ek_for_scalar_u64(v)` for building fixture pubkeys from `u64` scalars. Each maps to Lean `funcIdx := 40` (`ldTrue`) because correct algebra makes every row return `true`. **Runs (post-addition):** `cargo run -p move-lean-difftest -- --suite confidential_balance --suite confidential_elgamal --suite confidential_proof` + `lake exe difftest ...` → **416 passed / 0 failed** (up from 399). No production bug surfaced, but the entire "prover-side transcript drift" regression class — orthogonal to Phase H (verifier rejection) and to Phase G / N (verifier FS prefix) — is now pinned. |
| 2026-04-17 | **Phase N — individual-field-coverage FS prefix pins (17 rows, 0 latent bugs; previously-undetectable "single field silently dropped from transcript" regression class now covered).** Phase G pinned "chain_id / sender / contract matters" for each of the four FS prefixes (withdrawal / normalization / rotation / transfer) plus position SWAPS for adjacent slots (sender↔contract, sender_ek↔recipient_ek, current_balance↔new_balance, current_ek↔new_ek, auditor order). Position-swap pins catch "one of the two swapped slots was dropped", but they CANNOT distinguish "both slots are hashed" from "only one slot is hashed" from "both slots are dropped" — e.g., a regression that drops `new_ek` from the rotation transcript still makes the pair `(ek_g, ek_g)` vs `(ek_h, ek_g)` produce different bytes (because `current_ek` changed), so the swap pin still passes. Individually pinning each remaining field — hold every other field fixed, vary ONLY field X, require bytes differ — is strictly stronger: it forces field X to be hashed at all. **Pin strategy:** add `*_matters` pins for every non-trivially-hashed field of every FS prefix. 17 rows added: withdrawal adds `wd_{ek,current_balance}_matters` (amount_chunks already pinned in G.2); normalization adds `norm_{ek,current_balance,new_balance}_matters`; rotation adds `rot_{current_ek,new_ek,current_balance,new_balance}_matters`; transfer adds `tr_{sender_ek,recipient_ek,current_balance,new_balance,sender_amount,recipient_amount,auditor_ek_content,auditor_amount_content}_matters`. The last two target a specific bug class — hashing only auditor-vector COUNT but not CONTENTS — which the existing `auditor_count_matters` row cannot catch (it uses 0-vs-1 element counts, so a buggy transcript that hashes `len()` alone still varies). Each row maps to Lean `funcIdx := 40` (`ldTrue`). Uses factored helpers `nonzero_actual_bal_for_fs_tests()` (chunk-0 C = basepoint) and `nonzero_pending_bal_d_for_fs_tests()` (chunk-0 D = basepoint; matches production which hashes only D points of sender/recipient amounts). **Runs (post-addition):** `cargo run -p move-lean-difftest -- --suite confidential_balance --suite confidential_elgamal --suite confidential_proof` + `lake exe difftest ...` → **399 passed / 0 failed**. No production bug surfaced, but an entire regression class — "one input silently dropped from the Fiat-Shamir transcript" — is now pinned byte-for-byte. |
| 2026-04-17 | **Phase M — cross-type byte-length rejection pins for `new_{pending,actual}_balance_from_bytes` (4 rows, 0 latent bugs; previously-undetectable "balance-type length constants aliased / swapped" regression class now covered).** Before Phase M, the two balance deserializers `new_pending_balance_from_bytes` (enforces `== 256 B = 4 × 64`) and `new_actual_balance_from_bytes` (enforces `== 512 B = 8 × 64`) were individually pinned against off-by-one and wildly-wrong lengths (empty / `short` / `257` / `513`), but NO pin existed against the symmetric "other-type" length — i.e. feeding 512 canonical bytes to the pending parser, or 256 canonical bytes to the actual parser. This leaves a silent-accept regression class uncovered: a refactor that accidentally aliases `PENDING_BALANCE_CHUNKS` ↔ `ACTUAL_BALANCE_CHUNKS` in the length check (for example, via a constant rename, a shared helper, or an inlined view wrapper) would (i) pass the byte-length guard on the *other* type's canonical serialization, (ii) then slice only the first `PENDING_BALANCE_CHUNKS`-many (or `ACTUAL_BALANCE_CHUNKS`-many) 64-B windows, silently producing a mis-shaped `ConfidentialBalance` — a latent type-confusion vulnerability that would propagate into every downstream chunk-count assertion (see Phase L), and, because the produced `ConfidentialBalance` is otherwise well-formed, would escape any positive roundtrip test. **Pin strategy:** two raw-length rows (`test_pending_from_actual_size_zeros_is_none` feeding 512 zeros into the pending parser, `test_actual_from_pending_size_zeros_is_none` feeding 256 zeros into the actual parser) plus two end-to-end rows (`test_pending_from_actual_roundtrip_is_none` using `balance_to_bytes(new_actual_balance_no_randomness())` — the only production path that ever produces a 512-B canonical balance blob — and `test_actual_from_pending_roundtrip_is_none` dual). Each maps to Lean `funcIdx := 40` (`ldTrue`) because the Move wrapper evaluates `std::option::is_none(...)`. **Runs (post-addition):** `./difftest.sh --suite confidential` → **406 passed / 0 failed** (up from 402). No production bug surfaced, but a previously-undetectable type-confusion regression class is now pinned. |
| 2026-04-17 | **Phase L — length-mismatch hard-abort pins for the four chunk-sensitive `confidential_balance` helpers (6 rows, 0 latent bugs; previously-undetectable "chunk-length assertion dropped / downgraded" regression class now covered).** Before Phase L, the four chunk-sensitive helpers `balance_equals`, `balance_c_equals`, `add_balances_mut`, `sub_balances_mut` each carried an explicit `assert!(lhs.chunks.length() {==,>=} rhs.chunks.length(), error::internal(EINTERNAL_ERROR))` guard, but only the *positive* (equal-length) paths were exercised by existing rows. A regression that (i) dropped the assertion, (ii) downgraded the category to `error::invalid_argument(1) = 65537` (mis-classifying an invariant violation as a user-facing validation failure), or (iii) flipped `>=` to `<=` in `add_balances_mut` / `sub_balances_mut` would silently: for `balance_{,c_}equals` — process only the shorter chunk prefix and return a valid-looking `bool`, letting actual-vs-pending balance comparisons pass with ambiguous chunks; for `add_balances_mut` / `sub_balances_mut` — on a short-lhs call `pending += actual`, walk `lhs.chunks.enumerate_mut` but fail to visit `rhs.chunks[4..8]` (because `lhs.chunks.length() == 4`), silently truncating the sum / difference and corrupting actual-balance state. This class of bug is NOT caught by existing positive roundtrip tests because those tests use same-length inputs on both sides of the guard. **Pin strategy:** pass a 4-chunk pending and 8-chunk actual balance (both zero-plaintext, no-randomness) in each argument order and require the call to abort with canonical `error::internal(1) = 0x0B_0001 = 720897`, exactly matching the twin `test_bal_verify_{actual,pending}_rejects_*_length_aborts` rows already covering `verify_{actual,pending}_balance_for_test`. Added six rows: `test_bal_{balance_equals,balance_c_equals}_mismatched_chunks_{pending_actual,actual_pending}_aborts` (four) + `test_bal_{add,sub}_balances_mut_pending_{plus,minus}_actual_aborts` (two). Each maps to Lean `funcIdx := 196` (`aborted 720897`). **Runs (post-addition):** `./difftest.sh --suite confidential` → **402 passed / 0 failed** (up from 396 after the ElGamal non-canonical additions). No production bug surfaced, but a previously-undetectable harness-side regression class is now pinned. |
| 2026-04-17 | **Phase K — non-canonical scalar / point rejection pins for every sigma-proof deserializer (17 rows, 0 latent bugs; previously-undetectable "canonicality check dropped" regression now covered).** Phase J closed the length-based silent-accept class (`!= EXPECTED` vs `<`, `<=`, etc.). The other independent silent-accept class is *per-slot canonicality*: the sigma deserializers parse each 32-byte window via `ristretto255::new_scalar_from_bytes` (rejects when the encoded value ≥ `L` — the curve order) or `ristretto255::new_compressed_point_from_bytes` (rejects when `point_is_canonical_internal(bytes) == false` — including encodings with the high bit set, which no canonical ristretto255 point ever has). A future "optimization" that inlines the scalar/point constructors without re-checking canonicality — or a refactor that reorders the `alphas.any(|a| a.is_none())` / `xs.any(|x| x.is_none())` reduction and drops a slot — would silently parse non-canonical inputs, and downstream MSM / challenge derivation could then be driven into attacker-chosen regions (breaks 1-1 encoding contract → soundness loss). **Pin strategy:** build a zero-filled sigma of canonical length, then overwrite exactly ONE 32-byte window with `0xff * 32`. The all-`0xff` pattern fails BOTH scalar canonicality (`2^256 - 1 > L`) AND point canonicality (high bit set), so the same helper drives both classes of pin by varying only the offset. Targeting a "first slot" AND a "last slot" in both the scalar prefix and the point suffix catches loop-bound off-by-ones in either parse loop. Added helper `make_zero_bytes_with_ff_at(n, off)`; **17 rows** registered: four each for withdrawal / normalization / rotation (`bad_{first,last}_{scalar,point}`), four for transfer base layout (`bad_{first,last}_{scalar,point}`), and one extra `test_deserialize_transfer_sigma_bad_last_auditor_point_is_none` pinning the per-auditor point loop. Each maps to Lean `funcIdx := 40` (`ldTrue`). **Runs (post-addition):** `./difftest.sh --suite confidential` → **392 passed / 0 failed** (up from 375). No production bug surfaced. |
| 2026-04-17 | **Phase J — deserializer length-check regression pins (8 rows, 0 latent bugs; previously-undetectable "silently-accepts-too-long" regressions now covered).** The existing `test_deserialize_*_proof_one_byte_short_is_none` rows (added during the sigma layout corpora pass) catch regressions that *weaken* the length check from `!=` to `<=` or `<`, i.e. that start accepting lengths strictly shorter than the canonical witness. They do NOT catch the symmetric class of regressions where `!=` is weakened the other way — i.e. to `<`, which would silently accept `EXPECTED + k` bytes for any `k > 0` (the deserializer slices only `EXPECTED` bytes via `proof_bytes.slice(i * 32, (i + 1) * 32)`, so the trailing bytes are ignored; the proof deserializes as if it were `EXPECTED` bytes long). A "len too long" proof is indistinguishable from a genuine proof to downstream `verify_*` code — opens the door to signed-data smuggling / ambiguity attacks. **Pin:** feed `EXPECTED + 1` bytes and require `Option::None`. Added **three rows** for the fixed-length sigmas — `test_deserialize_{withdrawal,normalization,rotation}_proof_one_byte_too_long_is_none` (withdrawal / normalization canonical = 1152 B, rotation = 1216 B). **Transfer** has a two-part length check — `< 1792 OR auditor_xs % 128 != 0` — so the single-byte-too-long case is already rejected by the modulo; add **five rows** targeting each independent failure mode: `test_deserialize_transfer_proof_base_plus_{1,32,64,96}_is_none` (misaligned) and `test_deserialize_transfer_proof_base_minus_1_is_none` (too short). **Runs (post-addition):** `./difftest.sh --suite confidential` → **380 passed / 0 failed**. No production bug surfaced, but a previously-undetectable regression class is now pinned. Each row maps to Lean `funcIdx := 40` (`ldTrue`). |
| 2026-04-17 | **Phase I — `balance_equals` vs `balance_c_equals` distinction pins (5 rows, 0 latent bugs; previously-undetectable "collapse to C-only check" regression now covered).** Before Phase I, every existing test that used `balance_equals` did so either on the *same* balance twice (`self` equality) or on two balances that were byte-identical (e.g. two `new_pending_balance_no_randomness()`). This means a silent regression where `balance_equals` was rewritten to call `balance_c_equals` (collapsing the D comparison to a no-op, e.g. as an incorrect "optimization") would NOT have been caught: every existing pin still passes, because for the test inputs the D components agree trivially. That regression would be catastrophic — `verify_{pending,actual}_balance` uses `balance_equals` to tie the decrypted-amount's recomputed D components to the balance's D; collapsing that check to C-only leaves amount-with-arbitrary-D satisfying verify. Added helpers `make_nonzero_d_pending` (C = all-identity, D[0] = basepoint) and `make_nonzero_c_pending` (C[0] = basepoint, D = all-identity) built from `new_pending_balance_from_bytes` so the balances differ in exactly one component. Pinned `balance_c_equals(zero, nonzero_d) == true`, `balance_equals(zero, nonzero_d) == false` (both argument orders), `balance_c_equals(zero, nonzero_c) == false`, and `balance_equals(zero, nonzero_c) == false`. Runner mapping `funcIdx := 40` (`ldTrue`). **Runs (post-addition):** `./difftest.sh --suite confidential` → **367 passed / 0 failed** (from 362). |
| 2026-04-17 | **Phase H — registration-proof NEGATIVE pins (7 rows, 0 latent bugs; full transcript-binding coverage for the sole fully-round-tripped sigma).** Registration is the only sigma where both `prove_registration_deterministic_for_difftest` and `verify_registration_proof_for_difftest` are `public fun` (the other four sigmas have `#[test_only]` provers and are testable only via the positive `0x7::confidential_proof_tests::success_*` unit tests and our Phase D.1 all-zero-sigma negatives). Positive roundtrip tests cannot catch a whole class of "silent" regressions where a *single* input is silently dropped from the transcript (`chain_id`, `sender`, `contract_address`, `token_address`, `ek`) or the binding of `response` / `commitment` to the recomputed challenge is weakened: in a single-process test the prover and verifier both do the same thing, the equation still holds algebraically, the test passes, but in production the off-chain prover and on-chain verifier disagree (or worse, cross-chain / cross-user replay becomes possible). **Pin strategy:** prove a valid registration proof on fixture `(dk=42, k=9999)`, then mutate exactly ONE argument on the verifier (a non-equal address for address fields, a different scalar for `ek`, XOR of last byte / first byte for the commitment / response bytes), and require `verify_registration_proof_for_difftest` to abort with `error::invalid_argument(ESIGMA_PROTOCOL_VERIFY_FAILED) = 65537`. Added seven rows: `test_verify_registration_rejects_{sender,contract,token,chain_id,ek,commitment,response}_mutation`. Each maps to Lean `caSigmaVerifyFailedAbortDesc` at `funcIdx := 195`. **Runs (post-addition):** `./difftest.sh --suite confidential` → **362 passed / 0 failed** (from 355). |
| 2026-04-17 | **Phase G.2 — Fiat-Shamir transcript position-SWAP pins (10 rows, 0 latent bugs; catches regressions that positive prove/verify roundtrips cannot catch).** Building on Phase G, added pins specifically for the class of bug where a refactor SWAPS two adjacent arguments at a call site — e.g. `sender_ek` ↔ `recipient_ek`, `current_balance` ↔ `new_balance`, `sender` ↔ `contract_address` in `prepend_domain_context`, auditor list reversal. Positive roundtrip tests CANNOT catch these because both the prover and verifier (inside the same unit test) apply the same swap, compute the same (wrong) challenge on both sides, and the equation still holds algebraically — but in production, the off-chain prover (without the swap) and on-chain verifier (with the swap) would compute different challenges, and every transfer would fail (or worse, a role swap routes audit traffic to the wrong party undetectably). Added helper `hash_base_ek_for_fs_tests()` using `ristretto255::hash_to_point_base()` (a second, provably distinct ek from the basepoint); 10 rows: `test_fs_prefix_two_test_eks_are_distinct` (foundation — both test eks differ), `test_fs_prefix_wd_sender_vs_contract_swap_matters`, `test_fs_prefix_wd_amount_chunks_matter`, `test_fs_prefix_{norm,rot,tr}_*_swap_matters` (balance / ek / amount swaps), `test_fs_prefix_tr_auditor_eks_order_matters`. Each maps to Lean `funcIdx := 40` (`ldTrue`). Also fixed a latent doc inaccuracy in `test_deserialize_rotation_proof_one_byte_short_is_none` (used `n = 1215` for a 1216-byte rotation sigma; previously claimed `1183` for a non-existent `37 × 32 = 1184` length). **Runs (post-addition):** `./difftest.sh --suite confidential` → **355 passed / 0 failed** (from 345). |
| 2026-04-17 | **Phase G — Fiat-Shamir transcript prefix pins for the four sigma protocols (no latent bugs found; byte-level transcript layout now VM↔Lean-pinned).** Before this pass the Fiat–Shamir transcript construction used inside every `verify_*_proof` was only *transitively* validated — i.e. through the `success_*` roundtrip unit tests (`0x7::confidential_proof_tests`) which build the message, hash it, and verify a concrete proof all in one shot. A regression that (i) swapped the order of `sender` and `contract_address` inside `prepend_domain_context`, (ii) dropped the `G` or `H` point prepend, (iii) reused a sibling protocol's `FIAT_SHAMIR_*_DST` literal (the withdrawal/transfer/rotation DSTs are all the same length or near-length, catchable neither by byte-length pins nor by the existing DST-inequality rows if the bug were on the *consumer* side), or (iv) forgot to iterate the auditor-ek vector in the transfer transcript — would still silently pass every pre-existing positive roundtrip test (because the challenge would agree on both sides of the prover/verifier in that single test), but would break cross-party verification in production (off-chain prover and on-chain verifier would disagree). **Source refactor (byte-identical, no semantic change).** Factored out the "public-inputs prefix" of each challenge computation — `DST || chain_id || sender || contract || G || H || <pubkey(s)> || <balance(s)> || <amount chunks / auditor lists as applicable>` — into four new **private** helpers in `confidential_proof.move`: `fiat_shamir_{withdrawal,transfer,normalization,rotation}_sigma_proof_prefix`. Each existing `fiat_shamir_*_sigma_proof_challenge` now starts from that prefix and appends only the proof commitment points (`X_*`) and, for `transfer`, the trailing `bcs(sender_auditor_hint)`. This refactor is byte-identical to the pre-refactor transcript — verified by the full `0x7::confidential_proof_tests::{success_withdraw, success_transfer, success_transfer_with_non_empty_auditor_hint, success_normalize, success_rotate}` suite continuing to pass unchanged (happy-path proofs require the prover and verifier to compute the same challenge byte-for-byte; any reordering would break them). Added four corresponding **public** wrappers `{withdrawal,transfer,normalization,rotation}_fs_prefix_for_test` that expose the prefix for `move-lean-difftest` pinning (same precedent as the existing `registration_fs_message_for_test` at oracle index **171**). Because the public wrappers simply call the same private helper that the production verifier uses, **zero drift risk**: a future edit that changes the transcript layout must change the private helper, which flips both the verifier's challenge *and* the `_for_test` wrapper's output, caught by both the positive roundtrip tests *and* the new prefix-pin rows. **Added 24 VM↔Lean rows** (all `funcIdx := 40` / `ldTrue`): six per protocol covering (i) the prefix starts with that protocol's exact DST literal (`test_fs_prefix_*_starts_with_dst`), (ii) determinism under identical inputs (`_deterministic`), (iii) sensitivity to `chain_id` (`_chain_id_matters`), (iv) sensitivity to `sender` (`_sender_matters`), (v) sensitivity to `contract_address` (`_contract_matters`), and (vi) cross-protocol distinctness on structurally-shared inputs (`test_fs_prefix_wd_vs_norm_distinct`, `test_fs_prefix_rot_vs_norm_distinct`, `test_fs_prefix_tr_vs_wd_distinct`); plus `test_fs_prefix_tr_auditor_count_matters` catching a future refactor that drops the auditor-list iteration in the transfer transcript. **Runs (post-addition):** `./difftest.sh --suite confidential` → **345 passed / 0 failed** (up from 321). `cargo test -p aptos-framework --test move_unit_test --release` — all **64/64** `0x7::confidential_*` CA Move unit tests still pass (one pre-existing `0x1::staking_config` failure unrelated to CA / to this work). **§8.1 coverage-matrix** rows for the four `fiat_shamir_*_sigma_proof_challenge` transcript constructions upgraded from "private helper, transitively tested only" to "byte-exact VM↔Lean pinned (Phase G)". No latent production bugs surfaced by Phase G — every new row passes on the refactored source, and the refactor itself is byte-identical to the original by the existing positive roundtrip tests. The running bug-fixes log at [`inventory/bug_fixes_found_by_difftests.md`](bug_fixes_found_by_difftests.md) now records Phase G as a harness-only addition (no production bug fix, but a latent-bug *class* newly covered). |
| 2026-04-10 | Phase 0 inventory created; suite `confidential` not registered yet. |
| 2026-04-10 | Phases 1–2 + partial 3–4: suites `confidential_balance`, `confidential_proof`, `confidential_asset` + meta `confidential`; Lean `Programs/Confidential.lean`; **Phase 4 = Option B** (globals-free slice only). |
| 2026-04-12 | Documented **stretch** rows (store/FA entrypoints, full `verify_*` + registration bytecode, Lean bytecode parity) with pointers to plan **§7**; noted **§7.0** `e2e-move-tests` already covers transactional VM + real verifiers. |
| 2026-04-12 | `serialize_auditor_*` promoted to non-`#[test_only]` `public fun` on `confidential_asset`; difftest calls them from `0x1::difftest_confidential_asset_layer` (VM↔Lean unchanged on empty vectors). |
| 2026-04-12 | More `confidential_balance` VM rows (`balance_equals` / `balance_c_equals` / `sub_balances_mut` smoke); `get_fiat_shamir_registration_sigma_dst` `#[view]` on `confidential_proof`; Lean `Programs/Confidential` indices 47–51 (balance bool stubs + registration DST const). |
| 2026-04-12 | Lean **Phase L5 stub:** `MachineState.faBalances`, `faReadBalance` / `faWriteBalance`, `eval … initMs`, `fa_stub` Rust suite + confidential index 52 + Runner seed for `test_fa_stub_balance_answer`. |
| 2026-04-12 | Near-term CA difftest: Runner aliases for legacy `serialize_auditor_*_empty_mirror` names; `split_into_chunks_*` inventory clarified (**bool** harness, no `vector<Scalar>` in JSON); ElGamal **`ciphertext_add_assign`** VM row + Lean stub index **53**. |
| 2026-04-12 | ElGamal **`ciphertext_sub_assign`** harness + Lean stub index **54**. |
| 2026-04-12 | Batch: balance **`actual` roundtrip / `u64` zero / wrong-len `Option` / `balance_c` on `u64` zeros / add-two-actual-zeros**; ElGamal **`ciphertext_sub` self** (indices **55–60**; `ciphertext_clone` harness omitted — VM abort on head bundle). |
| 2026-04-12 | Balance: **`actual` 511-byte short len** (const pool **9** in Lean), actual **`sub` / `balance_equals` / `balance_c`**, **compressed pending → decompress vs plain zero** (indices **61–65**). |
| 2026-04-12 | ElGamal **`ciphertext_add` commutes** at zero ciphertexts (**66**); balance **self-`equals` on actual** + **`is_zero` after decompress compressed pending** (**67–68**). |
| 2026-04-12 | Compressed **actual** decompress vs plain zero + `is_zero`; **`balance_c`** on two actual zeros; ElGamal **three-way associativity** at zero (**69–72**); layer suite **`get_actual_balance_chunks` / `get_chunk_size_bits`** (Runner → indices **1** / **2**). |
| 2026-04-12 | Balance **BCS roundtrip** strengthened (`balance_equals` self pending/actual); **`balance_c_equals`** on two plain pending zeros; **`is_zero`** false for **`new_pending_balance_u64_no_randonmess(1)`**; ElGamal **`Option`** edges — short pubkey bytes, **63-byte** ciphertext (**73–78**). |
| 2026-04-12 | Large wrong-byte **`Option`** rows (**257** pending / **513** actual zeros) reuse Lean indices **9** / **57**; cross-constructor **`balance_equals` / `balance_c_equals` / add / sub`** (plain vs `u64(0)` pending); **`split_into_chunks_*` chunk 1** scalars; ElGamal **65-byte CT / 31-byte PK** + **sub→add** restore (**79–92**). |
| 2026-04-12 | **`split_into_chunks_u64`** chunk indices **2–3** and **`split_into_chunks_u128`** **2–5** (scalar equality); **`is_zero`** on actual **no-rand** balance after compress/decompress (**93–99**). `confidential_proof` **1-byte sigma** `deserialize_*` smoke (withdrawal / transfer / normalization / rotation) — Runner aliases to Lean **16–19** (same stub as all-empty `None` rows). |
| 2026-04-12 | **`split_into_chunks_u128`** chunk indices **6–7** (`<< 96` / `<< 112`); planning doc **§4.5** adds Tier **C** traceability vs repo (what exists vs **open** for true Lean replay of FA + full `verify_*`). |
| 2026-04-12 | E2e oracle row **`confidential_asset_rollover_and_freeze_only`** (register → deposit → `rollover_pending_balance_and_freeze`); merged CI oracle + Lean pass; **`move-lean-difftest` `default-run`** so `cargo run -p move-lean-difftest` / `difftest.sh` work with two binaries. |
| 2026-04-12 | E2e oracle row **`confidential_asset_rotate_encryption_key_and_unfreeze_only`** (`rollover_pending_balance_and_freeze` → `rotate_encryption_key_and_unfreeze`); helper **`run_rotate_and_unfreeze`** in `confidential_asset_e2e.rs`; Runner witness **funcIdx 40**. |
| 2026-04-12 | E2e oracle row **`confidential_asset_freeze_then_unfreeze_only`** (`freeze_token` → `unfreeze_token` after register+deposit); helpers **`run_freeze_token` / `run_unfreeze_token`**; Runner witness **funcIdx 40**. |
| 2026-04-12 | E2e oracle row **`confidential_asset_rollover_then_normalize_only`** (`rollover_pending_balance` → **`normalize`** with real `verify_normalization_proof`); Move **`pack_normalization_proof`** in `confidential_gas_e2e_helpers`; Rust **`run_normalize` / `pack_normalize`**; Runner witness **funcIdx 40**. |
| 2026-04-12 | E2e oracle rows **`confidential_asset_deposit_to_cross_party_only`** (`deposit_to` to a second registered account) and **`confidential_asset_withdraw_entry_self_only`** (**`withdraw`** self entry vs `withdraw_to`); helpers **`run_deposit_to` / `run_withdraw`**; Runner **funcIdx 40**. |
| 2026-04-12 | E2e oracle row **`confidential_asset_rotate_encryption_key_after_freeze_only`** — **`rotate_encryption_key`** entry alone after `rollover_pending_balance_and_freeze` (vs combined `rotate_encryption_key_and_unfreeze`); Runner **funcIdx 40**. |
| 2026-04-12 | E2e oracle row **`confidential_asset_is_normalized_false_after_rollover_only`** — VM `is_normalized` view after **`rollover_pending_balance`**; JSON **`bool(false)`**; Lean new witness **`funcIdx 102`** (`Programs/Confidential.lean`). |
| 2026-04-12 | E2e oracle row **`confidential_asset_is_frozen_true_after_freeze_token_only`** — VM **`is_frozen`** after **`freeze_token`**; JSON **`bool(true)`**; Runner **funcIdx 40**. |
| 2026-04-12 | E2e oracle row **`get_auditor_returns_none_for_move_metadata_no_fa_config_only`** — VM **`get_auditor(MOVE_METADATA)`** when allow-list off and no **`FAConfig`**; Rust asserts BCS **`[0]`** (`none`); JSON **`bool(true)`**; Runner **40**. |
| 2026-04-12 | E2e oracle row **`actual_balance_view_return_len_529_after_register_only`** — VM **`actual_balance`** after **`register`**; Rust asserts return length **529**; JSON **`bool(true)`**; Runner **40**. |
| 2026-04-12 | E2e oracle row **`pending_balance_view_return_len_265_after_register_only`** — VM **`pending_balance`** after **`register`** (no deposit); Rust asserts serialized return length **265** (`bypass_at` framing); JSON **`bool(true)`**; Runner **40**. |
| 2026-04-12 | E2e oracle row **`encryption_key_view_matches_registered_ek_only`** — VM **`encryption_key`** `#[view]` after **`register`**; Rust asserts **`pubkey_to_bytes(view_return)`** equals registration **`pubkey_to_bytes(ek_struct)`**; JSON **`bool(true)`**; Runner **40**. |
| 2026-04-12 | E2e oracle row **`verify_pending_balance_zero_after_register_only`** — VM **`verify_pending_balance`** (**`bypass_at`**) after **`register`** only with **`u64(0)`** + **`dk`**; Rust asserts **`bool(true)`**; JSON **`bool(true)`**; Runner **40**. |
| 2026-04-12 | E2e oracle rows **`verify_pending_balance_rejects_nonzero_after_register_only`** / **`verify_actual_balance_rejects_nonzero_after_register_only`** — **`verify_{pending,actual}_balance(1)`** after **`register`** only ⇒ **`bool(false)`**; Runner **102**. |
| 2026-04-12 | E2e oracle row **`verify_actual_balance_zero_after_register_only`** — VM **`verify_actual_balance`** (**`bypass_at`**) after **`register`** only with **`u128(0)`** + **`dk`**; Rust asserts **`bool(true)`**; JSON **`bool(true)`**; Runner **40** (merged e2e success pin). |
| 2026-04-12 | E2e oracle row **`verify_actual_balance_matches_after_deposit_and_rollover_only`** — VM **`deposit(888)`** → **`rollover_pending_balance`** → **`verify_actual_balance`** with **`u128(888)`** + **`dk`**; Rust asserts **`bool(true)`**; JSON **`bool(true)`**; Runner **40**. |
| 2026-04-12 | E2e oracle row **`verify_pending_balance_zero_after_deposit_and_rollover_only`** — same **`deposit(888)`** + **`rollover`**, then **`verify_pending_balance(0)`** + **`dk`**; JSON **`bool(true)`**; Runner **40**. |
| 2026-04-12 | E2e oracle rows **`verify_pending_balance_matches_after_deposit_only_no_rollover`** (**`deposit(333)`**, then **`verify_pending_balance(333)`**) and **`verify_actual_balance_zero_after_deposit_only_no_rollover`** (same deposit, then **`verify_actual_balance(0)`** before rollover); JSON **`bool(true)`**; Runner **40**. |
| 2026-04-12 | E2e oracle row **`verify_pending_balance_matches_sum_after_two_deposits_no_rollover`** — **`deposit(100)`** + **`deposit(200)`**, **`verify_pending_balance(300)`** ⇒ **`bool(true)`**; Runner **40**. |
| 2026-04-10 | E2e oracle row **`verify_pending_balance_rejects_zero_after_two_deposits_no_rollover`** — **`deposit(11)`** + **`deposit(22)`**, **`verify_pending_balance(0)`** vs pending **33** before rollover ⇒ **`bool(false)`**; Runner **102**. |
| 2026-04-10 | E2e oracle row **`verify_pending_balance_rejects_zero_after_deposit_only_no_rollover`** — **`deposit(55)`**, **`verify_pending_balance(0)`** vs pending **55** before rollover ⇒ **`bool(false)`**; Runner **102**. |
| 2026-04-12 | E2e oracle rows **`verify_pending_balance_rejects_wrong_sum_after_two_deposits_no_rollover`** (**`299`** vs **300**) / **`verify_actual_balance_matches_sum_after_two_deposits_and_rollover_only`** (**`40+60`**, **`verify_actual_balance(100)`** after **`rollover`**) ⇒ **`bool(false)`** / **`bool(true)`**; Runner **102** / **40**. |
| 2026-04-12 | E2e oracle row **`verify_actual_balance_rejects_wrong_sum_after_two_deposits_and_rollover_only`** — **`deposit(50)`** + **`deposit(70)`** + **`rollover`**, **`verify_actual_balance(119)`** vs **actual `120`** ⇒ **`bool(false)`**; Runner **102**. |
| 2026-04-12 | E2e oracle row **`verify_actual_balance_rejects_wrong_amount_after_deposit_and_rollover_only`** — **`deposit(888)`** + **`rollover`**, **`verify_actual_balance(887)`** ⇒ **`bool(false)`**; Runner **102**. |
| 2026-04-12 | E2e oracle row **`verify_pending_balance_rejects_wrong_amount_after_deposit_only_no_rollover`** — **`deposit(333)`**, **`verify_pending_balance(332)`** ⇒ **`bool(false)`**; Runner **102**. |
| 2026-04-12 | E2e oracle row **`verify_actual_balance_rejects_nonzero_after_deposit_only_no_rollover`** — **`deposit(555)`** without rollover, **`verify_actual_balance(555)`** (pending-only) ⇒ **`bool(false)`**; Runner **102**. |
| 2026-04-10 | E2e oracle row **`verify_actual_balance_rejects_nonzero_sum_after_two_deposits_no_rollover`** — **`deposit(77)`** + **`deposit(88)`** without rollover, **`verify_actual_balance(165)`** (sum still in **pending**) ⇒ **`bool(false)`**; Runner **102**. |
| 2026-04-10 | E2e oracle row **`verify_actual_balance_rejects_wrong_sum_after_two_deposits_no_rollover`** — **`deposit(50)`** + **`deposit(60)`**, **`verify_actual_balance(109)`** vs pending **110** before rollover ⇒ **`bool(false)`**; Runner **102**. |
| 2026-04-10 | E2e oracle row **`verify_actual_balance_rejects_sum_plus_one_after_two_deposits_no_rollover`** — **`deposit(33)`** + **`deposit(44)`**, **`verify_actual_balance(78)`** vs pending **77** before rollover ⇒ **`bool(false)`**; Runner **102**. |
| 2026-04-12 | E2e oracle row **`verify_pending_balance_rejects_nonzero_after_deposit_and_rollover_only`** — **`deposit(888)`** + **`rollover`**, **`verify_pending_balance(1)`** ⇒ **`bool(false)`**; Runner **102**. |
| 2026-04-12 | E2e oracle rows **`verify_pending_balance_rejects_stale_deposit_amount_after_deposit_and_rollover_only`** / **`verify_actual_balance_rejects_zero_after_deposit_and_rollover_when_actual_nonzero`** — stale **`u64(777)`** on **pending** or **`u128(0)`** on **actual** after **`rollover`** ⇒ **`bool(false)`**; Runner **102**. |
| 2026-04-12 | E2e oracle row **`verify_pending_balance_rejects_stale_sum_after_two_deposits_and_rollover_only`** — **`deposit(41)`** + **`deposit(59)`** + **`rollover`**, **`verify_pending_balance(100)`** with cleared **pending** ⇒ **`bool(false)`**; Runner **102**. |
| 2026-04-12 | E2e oracle row **`verify_pending_balance_rejects_wrong_amount_after_two_deposits_and_rollover_only`** — **`deposit(40)`** + **`deposit(60)`** + **`rollover`**, **`verify_pending_balance(99)`** vs cleared **pending** ⇒ **`bool(false)`**; Runner **102**. |
| 2026-04-12 | E2e oracle rows **`has_confidential_asset_store_false_before_register_only`** / **`…_true_after_register_only`** — VM **`has_confidential_asset_store`**; JSON **`bool(false)`** / **`bool(true)`**; Runner **102** / **40**. |
| 2026-04-12 | E2e view oracle batch: **`is_token_allowed_true_for_metadata_only`**, **`is_allow_list_enabled_false_in_tests_only`** (non-mainnet genesis), **`is_normalized_true_after_register_only`**, **`is_frozen_false_after_unfreeze_only`**; Runner **40** / **102** / **40** / **102**. |
| 2026-04-12 | E2e view rows **`is_frozen_false_after_register_only`**, **`has_confidential_asset_store_false_for_peer_not_registered`** (Alice registered, Bob not); JSON **`bool(false)`**; Runner **102**. |
| 2026-04-12 | E2e view rows **`is_frozen_true_after_rollover_and_freeze_only`** (`rollover_pending_balance_and_freeze`), **`is_normalized_true_after_normalize_only`** (post-`normalize` after rollover); JSON **`bool(true)`**; Runner **40**. |
| 2026-04-12 | E2e oracle **`confidential_asset_balance_matches_single_deposit_only`** — VM **`confidential_asset_balance`** `#[view]` after **register + deposit(77)**; JSON **`u64(77)`**; Lean witness **`funcIdx 103`** (constant bytecode; not a full FA model). |
| 2026-04-12 | E2e oracle **`confidential_asset_balance_after_two_deposits_only`** — two self-deposits **100+65** ⇒ **`u64(165)`**; Lean **`funcIdx 104`**. |
| 2026-04-12 | E2e oracle **`confidential_asset_balance_after_deposit_and_withdraw_only`** — **`deposit(1000)`** → **`rollover`** → **`withdraw(333)`** ⇒ pool **`u64(667)`**; Lean **`funcIdx 105`**. |
| 2026-04-10 | E2e oracle **`verify_actual_balance_matches_after_deposit_rollover_and_withdraw_only`** — same **`1000` / `rollover` / `withdraw(333)`** path, **`verify_actual_balance(667)`** + **`dk`** ⇒ **`bool(true)`**; Runner **40** (merged e2e success pin). |
| 2026-04-10 | E2e oracle **`verify_actual_balance_rejects_wrong_amount_after_deposit_rollover_and_withdraw_only`** — same path, **`verify_actual_balance(668)`** vs pool **667** ⇒ **`bool(false)`**; Runner **102**. |
| 2026-04-10 | E2e oracle **`verify_pending_balance_zero_after_deposit_rollover_and_withdraw_only`** — same path, **`verify_pending_balance(0)`** ⇒ **`bool(true)`**; Runner **40**. |
| 2026-04-10 | E2e oracle rows **`verify_actual_balance_matches_after_deposit_rollover_and_normalize_only`** (**`deposit(424)`** + **`rollover`** + **`normalize`**, **`verify_actual_balance(424)`**) / **`verify_pending_balance_zero_after_deposit_rollover_and_normalize_only`** (**`deposit(515)`**, same path, **`verify_pending_balance(0)`**) ⇒ **`bool(true)`** each; Runner **40**. |
| 2026-04-10 | E2e oracle rows **`verify_actual_balance_rejects_wrong_amount_after_deposit_rollover_and_normalize_only`** (**`302`** vs **`303`** after **`normalize`**) / **`verify_pending_balance_rejects_nonzero_after_deposit_rollover_and_normalize_only`** (**`verify_pending_balance(1)`** with zero **pending**) ⇒ **`bool(false)`** each; Runner **102**. |
| 2026-04-10 | E2e oracle **`verify_pending_balance_rejects_stale_deposit_amount_after_deposit_rollover_and_normalize_only`** — **`deposit(707)`** + **`rollover`** + **`normalize`**, **`verify_pending_balance(707)`** with cleared **pending** ⇒ **`bool(false)`**; Runner **102**. |
| 2026-04-13 | E2e oracle rows **`verify_actual_balance_rejects_amount_plus_one_after_deposit_rollover_and_normalize_only`** (**`809`** vs **`808`** after **`normalize`**) / **`verify_actual_balance_rejects_zero_after_deposit_rollover_and_normalize_when_actual_nonzero`** (**`u128(0)`** vs **actual `919`**) ⇒ **`bool(false)`** each; Runner **102**. |
| 2026-04-13 | E2e oracle **`encryption_key_view_matches_new_ek_after_deposit_rollover_and_rotate_only`** — **`deposit(2112)`** + **`rollover`** + **`rotate_encryption_key`**, **`encryption_key`** view matches **new** EK bytes ⇒ **`bool(true)`**; Runner **40**. |
| 2026-04-13 | E2e oracle rows **`verify_actual_balance_matches_after_deposit_rollover_and_rotate_only`** (**new `dk`**) / **`verify_actual_balance_rejects_stale_dk_after_deposit_rollover_and_rotate_only`** (pre-rotate **`dk`**) post-**`rotate`** ⇒ **`bool(true)`** / **`bool(false)`**; Runner **40** / **102**. |
| 2026-04-13 | E2e oracle rows post-**`rotate`**: **`verify_pending_balance_zero_after_deposit_rollover_and_rotate_only`** (**new `dk`**) / **`verify_pending_balance_rejects_nonzero_with_stale_dk_after_deposit_rollover_and_rotate_only`** (**`verify_pending_balance(1)`**, stale **`dk`**) / **`verify_actual_balance_rejects_wrong_amount_after_deposit_rollover_and_rotate_only`** (**`actual−1`**, new **`dk`**) ⇒ **`bool(true)`** / **`bool(false)`** / **`bool(false)`**; Runner **40** / **102** / **102**. |
| 2026-04-13 | E2e oracle **`verify_pending_balance_rejects_nonzero_with_new_dk_after_deposit_rollover_and_rotate_only`** — **`verify_pending_balance(1)`** with **new** **`dk`**, zero **pending** ⇒ **`bool(false)`**; Runner **102**. |
| 2026-04-10 | E2e oracle batch post-**`rotate`**: **`verify_pending_balance_rejects_stale_deposit_amount_after_deposit_rollover_and_rotate_only`** (stale **`u64(deposit)`** on **pending**), **`verify_actual_balance_rejects_zero_after_deposit_rollover_and_rotate_when_actual_nonzero_only`**, **`verify_pending_balance_rejects_wrong_amount_after_deposit_rollover_and_rotate_only`** (**`deposit−1`**), **`verify_actual_balance_rejects_amount_plus_one_after_deposit_rollover_and_rotate_only`** — each **`bool(false)`**; Runner **102**. |
| 2026-04-10 | E2e oracle batch **`withdraw`** / **two-`deposit`** + **`rotate`**: **`verify_actual_balance_matches_after_deposit_rollover_withdraw_and_rotate_only`**, **`verify_actual_balance_matches_sum_after_two_deposits_rollover_and_rotate_only`**, **`verify_pending_balance_zero_after_deposit_rollover_withdraw_and_rotate_only`** (**`bool(true)`** / **40**); **`verify_actual_balance_rejects_stale_dk_after_deposit_rollover_withdraw_and_rotate_only`**, **`verify_pending_balance_rejects_stale_sum_after_two_deposits_rollover_and_rotate_only`**, **`verify_actual_balance_rejects_wrong_amount_after_deposit_rollover_withdraw_and_rotate_only`** (**`bool(false)`** / **102**). |
| 2026-04-10 | E2e oracle batch **`normalize`** + **`rotate`** (after **`deposit`**+**`rollover`**): **`verify_actual_balance_matches_after_deposit_rollover_normalize_and_rotate_only`**, **`encryption_key_view_matches_new_ek_after_deposit_rollover_normalize_and_rotate_only`**, **`verify_pending_balance_zero_after_deposit_rollover_normalize_and_rotate_only`** (**40**); **`verify_actual_balance_rejects_stale_dk_after_deposit_rollover_normalize_and_rotate_only`**, **`verify_pending_balance_rejects_nonzero_with_new_dk_after_deposit_rollover_normalize_and_rotate_only`**, **`verify_actual_balance_rejects_wrong_amount_after_deposit_rollover_normalize_and_rotate_only`** (**102**). |
| 2026-04-10 | E2e oracle batch **`rollover_pending_balance_and_freeze`** + **`rotate_encryption_key`** (no unfreeze): **`verify_actual_balance_matches_after_deposit_rollover_and_freeze_and_rotate_only`**, **`encryption_key_view_matches_new_ek_after_deposit_rollover_and_freeze_and_rotate_only`**, **`verify_pending_balance_zero_after_deposit_rollover_and_freeze_and_rotate_only`**, **`is_frozen_true_after_deposit_rollover_and_freeze_and_rotate_only`** (**40**); **`verify_actual_balance_rejects_stale_dk_after_deposit_rollover_and_freeze_and_rotate_only`**, **`verify_pending_balance_rejects_nonzero_with_new_dk_after_deposit_rollover_and_freeze_and_rotate_only`**, **`verify_actual_balance_rejects_wrong_amount_after_deposit_rollover_and_freeze_and_rotate_only`** (**102**). |
| 2026-04-10 | E2e oracle batch **`rollover_pending_balance_and_freeze`** + **`rotate_encryption_key_and_unfreeze`** (post-combined entry **`verify_*` / `is_frozen` / `encryption_key`**): six new **`confidential_asset_e2e::…`** rows (**40** for **`bool(true)`** pins, **102** for **`bool(false)`** including **`is_frozen`**); Lean **`RunnerFuncMappingAux`**. |
| 2026-04-10 | E2e oracle extension post-**`rotate_encryption_key_and_unfreeze`**: wrong **`verify_actual_balance`** (**`actual−1`** / **`actual+1`**) ⇒ **`bool(false)`** / **102**; **`is_normalized`** **`bool(true)`** / **40**. |
| 2026-04-10 | E2e oracle rows post-**`rotate_encryption_key_and_unfreeze`** + **second `deposit`**: **`verify_pending_balance(second)`** / **`verify_actual_balance(first)`** ⇒ **`bool(true)`** / **40**; **`verify_pending_balance(0)`** ⇒ **`bool(false)`** / **102**; **`verify_actual_balance(0)`** right after unfreeze (non-zero **actual**) ⇒ **`bool(false)`** / **102**. |
| 2026-04-10 | FV: **`Move.Step`** **`bytecodeLdU64AbortModuleEnv`** + **`eval_bytecodeLdU64AbortModuleEnv_aborted_*`**; **`Refinement.Confidential`** **`ca_e2e_abort_*_eq_eval_minimal_ldU64_abort_bytecode`** (**42** / **176** vs minimal **`eval`**). |
| 2026-04-10 | E2e oracle **`balance_after_deposit_rollover_freeze_and_rotate_encryption_key_and_unfreeze_only`** — pool **`u64(8881)`**; Lean **177**; **`Move.Step`** **`bytecodeLdU64RetModuleEnv`** + **`Refinement`** **`ca_e2e_balance_*`** / **`Tests.Confidential`** **`evalCA_177_eq_eval`**. |
| 2026-04-10 | E2e oracle rows: **`pending_balance`** / **`actual_balance`** view wire lengths **265** / **529** post-**`rotate_encryption_key_and_unfreeze`**; **`has_confidential_asset_store`** **`true`**; stale **`verify_pending_balance`** after second post-unfreeze **`deposit`** — **40** / **40** / **40** / **102**. |
| 2026-04-10 | E2e oracle extension same path: **`balance_matches_10003_after_post_unfreeze_deposit_post_rotate_encryption_key_and_unfreeze_only`** (**`u64(10003)`**; Lean **178**; **`Move.Step`** / **`Refinement`** / **`Tests.Confidential`** **`evalCA_178_eq_eval`**); **`is_token_allowed_true_…`**, **`get_auditor_returns_none_…`** (**`[0]`** BCS; oracle **`bool(true)`**), **`verify_pending_balance_matches_sum_after_two_post_unfreeze_deposits_…`** (**2000**+**900** pending) — Runner **40** / **40** / **40**. |
| 2026-04-10 | E2e oracle batch same two post-unfreeze **`deposit`** path: **`is_allow_list_enabled_false_after_deposit_rollover_freeze_and_rotate_encryption_key_and_unfreeze_only`**, **`verify_pending_balance_rejects_wrong_sum_after_two_post_unfreeze_deposits_…`** (**2899** vs **2900**), **`verify_actual_balance_rejects_wrong_amount_after_two_post_unfreeze_deposits_…`** (**`u128(6000)`** vs rolled **6001**) — Runner **102**; **`balance_matches_8901_after_two_post_unfreeze_deposits_…`** — **`u64(8901)`**; Lean **179**; **`evalCA_179_eq_eval`**. |
| 2026-04-10 | E2e oracle batch: **`verify_pending_balance_rejects_sum_plus_one_after_two_post_unfreeze_deposits_…`** (**2901**), **`verify_actual_balance_rejects_amount_plus_one_after_two_post_unfreeze_deposits_…`** (**6002**), **`is_frozen_false_after_two_post_unfreeze_deposits_…`** — **102**; **`encryption_key_view_matches_new_ek_after_two_post_unfreeze_deposits_…`** — **40**; **`balance_matches_6601_after_three_post_unfreeze_deposits_…`** (**6001+100+200+300**) — Lean **180** / **`evalCA_180_eq_eval`**; **`Move.State.lookupFaBalance_empty`**. |
| 2026-04-10 | E2e oracle batch (three then four post-unfreeze **`deposit`**s on rolled **6001** path): **`is_normalized_true_after_three_post_unfreeze_deposits_…`**, **`has_confidential_asset_store_true_after_three_…`**, **`verify_pending_balance_matches_sum_after_three_…`** (**600**) — Runner **40**; **`verify_pending_balance_rejects_zero_after_three_…`**, **`verify_actual_balance_rejects_zero_after_three_…`** — **102**; **`balance_matches_7111_after_four_post_unfreeze_deposits_…`** — Lean **181** / **`evalCA_181_eq_eval`**; **`Move.Step.eval_bytecodeLdU64RetModuleEnv_u64_7111`**; **`Refinement.Confidential`** **`ca_e2e_balance_u64_7111_*`** + **`102`** witness blurb extension. |
| 2026-04-10 | E2e oracle **`rotate_encryption_key_aborts_when_pending_nonzero_after_deposit_rollover_and_second_deposit_only`** — **`rollover_pending_balance`** then second **`deposit`** ⇒ non-zero **pending** ⇒ **`rotate_encryption_key`** **`MoveAbort`** (**196617**); Lean **176** (`caE2eAbort196617Desc`); **`Refinement.Confidential`** **`ca_e2e_abort_196617_eval_eq_aborted`**. |
| 2026-04-12 | E2e oracle **`confidential_asset_balance_after_deposit_to_only`** — single **`deposit_to`** **5678** ⇒ pool **`u64(5678)`**; Lean **`funcIdx 106`**. |
| 2026-04-12 | E2e oracle **`confidential_asset_balance_after_confidential_transfer_only`** — **`deposit(12345)`** → **`rollover`** → **`confidential_transfer(4321)`** to Bob ⇒ pool still **`u64(12345)`**; Lean **`funcIdx 107`**. |
| 2026-04-12 | E2e oracle **`confidential_asset_balance_after_transfer_and_second_deposit_only`** — **`deposit(5000)`** → **`rollover`** → **`confidential_transfer(1000)`** → **`deposit(2000)`** ⇒ pool **`u64(7000)`**; Lean **`funcIdx 108`**. |
| 2026-04-12 | E2e oracle **`confidential_asset_balance_after_two_deposit_to_only`** — two **`deposit_to`** **3333** + **4444** to same recipient ⇒ **`u64(7777)`**; Lean **`funcIdx 109`**. |
| 2026-04-12 | **Dual-track docs:** CA differential plan + formal verification plan both state **difftest + FV** as the peer-reviewable program; **§4.5 (iii)** scaffold [`corpora/confidential_assets/README.md`](../corpora/confidential_assets/README.md) for independent vectors. |
| 2026-04-12 | **§4.5 (iii) partial:** [`corpora/confidential_assets/`](../corpora/confidential_assets/) — registration FS `msg` goldens **1+2** as `.hex` + `.meta.json`; **`TranscriptAlignment`** theorems **`*_byte_length`** (199, DST prefix + transcript) + derived lengths for `registrationFiatShamirMsg` goldens. |
| 2026-04-12 | **Corpus + tooling:** `registration_sha2_512_golden_1.hex` (64 B SHA2-512); **`verify-corpora`** recomputes `SHA2-512(DST ‖ msg)` vs golden; **`TranscriptAlignment`** theorems **`expectedTaggedHashGolden_byte_length`** / **`tagged_hash_golden_msg_byte_length`**. |
| 2026-04-12 | **CI + `difftest.sh`:** **`verify-corpora`** runs as **formal-difftest** workflow step and as **`difftest.sh` step \[0\]** before VM oracle. |
| 2026-04-12 | **FV Workstream A (partial):** [`confidential_native_matrix.md`](confidential_native_matrix.md) — Ristretto/BP/hash vs CA; **`VerifyMath.fiatShamirRegistrationDst_byte_length`**; corpus **`fiat_shamir_registration_dst.hex`** + verifier checks DST drift. |
| 2026-04-12 | **BP DST corpus + proofs:** `bulletproofs_dst.hex` / `bulletproofs_dst_sha3_512.hex` (BP-specific SHA3-512; FS challenges use SHA2-512); **`Programs.Confidential`** `bulletproofsDstBytes_length` (44) / `bulletproofsDstSha3Bytes_length` (64); verifier extended; native matrix §6 (stub index map). |
| 2026-04-12 | **`deserialize_*` layout `Some`:** harness **`test_deserialize_{withdrawal,normalization,rotation,transfer}_layout_ok_is_some`** (VM); Lean **110–113** upgraded from `ldTrue` to **real `Step`** (same as **128–130**: corpus `ldConst` + `vecLen` + `eq`); differential plan **§4.3(iv)** still **partial** (VM `Some` stronger than Lean length check; full parser replay open). |
| 2026-04-12 | **Sigma layout corpora:** `deserialize_sigma_18_scalars_18_points.hex` (1152 B), `deserialize_sigma_19_scalars_19_points.hex` (1216 B), `deserialize_sigma_transfer_26_scalars_30_points.hex` (1792 B); **`verify-corpora`** checks vs canonical zero scalar + **A_POINT**; **`Programs.Confidential`** `deserializeSigma*Bytes` + **`*_length`** theorems. |
| 2026-04-12 | **Transfer sigma + one auditor quad (1920 B):** corpus `deserialize_sigma_transfer_26_scalars_30_points_plus_one_auditor_quad.hex`; harness **`test_layout_sigma_transfer_one_auditor_quad_extension_byte_length_is_1920`** + **`test_deserialize_transfer_layout_extended_one_auditor_ok_is_some`**; Lean **131–132** (`ldConst` **27** + `vecLen` + `eq`); **`deserializeSigmaTransfer26Scalars30PointsPlusOneAuditorQuadBytes`** + **`verify-corpora`**. |
| 2026-04-12 | **Transfer sigma + two auditor quads (2048 B):** corpus `deserialize_sigma_transfer_26_scalars_30_points_plus_two_auditor_quads.hex`; harness **`test_layout_sigma_transfer_two_auditor_quads_extension_byte_length_is_2048`** + **`test_deserialize_transfer_layout_extended_two_auditors_ok_is_some`**; Lean **133–134** (`ldConst` **28**); **`deserializeSigmaTransfer26Scalars30PointsPlusTwoAuditorQuadsBytes`** + prefix lemma vs one-quad wire. |
| 2026-04-12 | **Transfer sigma + three auditor quads (2176 B):** corpus `deserialize_sigma_transfer_26_scalars_30_points_plus_three_auditor_quads.hex`; harness **`test_layout_sigma_transfer_three_auditor_quads_extension_byte_length_is_2176`** + **`test_deserialize_transfer_layout_extended_three_auditors_ok_is_some`**; Lean **135–136** (`ldConst` **29**); **`deserializeSigmaTransfer26Scalars30PointsPlusThreeAuditorQuadsBytes`** + prefix lemmas vs two-quad / base wires. |
| 2026-04-12 | **Transfer sigma + four auditor quads (2304 B):** corpus `deserialize_sigma_transfer_26_scalars_30_points_plus_four_auditor_quads.hex`; harness **`test_layout_sigma_transfer_four_auditor_quads_extension_byte_length_is_2304`** + **`test_deserialize_transfer_layout_extended_four_auditors_ok_is_some`**; Lean **137–138** (`ldConst` **30**); **`deserializeSigmaTransfer26Scalars30PointsPlusFourAuditorQuadsBytes`** + prefix lemmas chaining to smaller extension tiers. |
| 2026-04-12 | **Transfer sigma + five / six auditor quads (2432 B / 2560 B):** corpora `deserialize_sigma_transfer_26_scalars_30_points_plus_five_auditor_quads.hex` / `deserialize_sigma_transfer_26_scalars_30_points_plus_six_auditor_quads.hex`; harness length + **`deserialize_transfer`** `Some` rows; Lean **139–142** (`ldConst` **31** / **32**); **`deserializeSigmaTransfer26Scalars30PointsPlusFiveAuditorQuadsBytes`** / **`deserializeSigmaTransfer26Scalars30PointsPlusSixAuditorQuadsBytes`** + prefix lemmas. |
| 2026-04-12 | **Transfer sigma + seven auditor quads (2688 B):** corpus `deserialize_sigma_transfer_26_scalars_30_points_plus_seven_auditor_quads.hex`; harness **`test_layout_sigma_transfer_seven_auditor_quads_extension_byte_length_is_2688`** + **`test_deserialize_transfer_layout_extended_seven_auditors_ok_is_some`**; Lean **143–144** (`ldConst` **33**); **`deserializeSigmaTransfer26Scalars30PointsPlusSevenAuditorQuadsBytes`** + prefix lemmas vs six-quad tier. |
| 2026-04-12 | **Transfer sigma + eight auditor quads (2816 B):** corpus `deserialize_sigma_transfer_26_scalars_30_points_plus_eight_auditor_quads.hex`; harness **`test_layout_sigma_transfer_eight_auditor_quads_extension_byte_length_is_2816`** + **`test_deserialize_transfer_layout_extended_eight_auditors_ok_is_some`**; Lean **145–146** (`ldConst` **34**); **`deserializeSigmaTransfer26Scalars30PointsPlusEightAuditorQuadsBytes`** + prefix lemmas vs seven-quad tier. |
| 2026-04-12 | **Transfer sigma + nine auditor quads (2944 B):** corpus `deserialize_sigma_transfer_26_scalars_30_points_plus_nine_auditor_quads.hex`; harness **`test_layout_sigma_transfer_nine_auditor_quads_extension_byte_length_is_2944`** + **`test_deserialize_transfer_layout_extended_nine_auditors_ok_is_some`**; Lean **147–148** (`ldConst` **35**); **`deserializeSigmaTransfer26Scalars30PointsPlusNineAuditorQuadsBytes`** + prefix lemmas vs eight-quad tier. |
| 2026-04-12 | **Transfer sigma + ten auditor quads (3072 B):** corpus `deserialize_sigma_transfer_26_scalars_30_points_plus_ten_auditor_quads.hex`; harness **`test_layout_sigma_transfer_ten_auditor_quads_extension_byte_length_is_3072`** + **`test_deserialize_transfer_layout_extended_ten_auditors_ok_is_some`**; Lean **149–150** (`ldConst` **36**); **`deserializeSigmaTransfer26Scalars30PointsPlusTenAuditorQuadsBytes`** + prefix lemmas vs nine-quad tier. |
| 2026-04-12 | **Transfer sigma + eleven auditor quads (3200 B):** corpus `deserialize_sigma_transfer_26_scalars_30_points_plus_eleven_auditor_quads.hex`; harness **`test_layout_sigma_transfer_eleven_auditor_quads_extension_byte_length_is_3200`** + **`test_deserialize_transfer_layout_extended_eleven_auditors_ok_is_some`**; Lean **151–152** (`ldConst` **37**); **`deserializeSigmaTransfer26Scalars30PointsPlusElevenAuditorQuadsBytes`** + prefix lemmas vs ten-quad tier. |
| 2026-04-12 | **Transfer sigma + twelve auditor quads (3328 B):** corpus `deserialize_sigma_transfer_26_scalars_30_points_plus_twelve_auditor_quads.hex`; harness **`test_layout_sigma_transfer_twelve_auditor_quads_extension_byte_length_is_3328`** + **`test_deserialize_transfer_layout_extended_twelve_auditors_ok_is_some`**; Lean **153–154** (`ldConst` **38**); **`deserializeSigmaTransfer26Scalars30PointsPlusTwelveAuditorQuadsBytes`** + prefix lemmas vs eleven-quad tier; **`Refinement.Confidential`** **`sigma_transfer_ext3328_len_eval_eq`**; Fiat–Shamir DST + empty serializer **`mvU8Wire`** bundles. |
| 2026-04-12 | **Transfer sigma + thirteen auditor quads (3456 B):** corpus `…_plus_thirteen_auditor_quads.hex`; harness length + **`deserialize_transfer`** `Some` rows; Lean **155–156** (`ldConst` **39**); **`deserializeSigmaTransfer26Scalars30PointsPlusThirteenAuditorQuadsBytes`** + prefix lemmas; **`Refinement.Confidential`** **`sigma_transfer_ext3456_len_eval_eq`**. |
| 2026-04-12 | **Transfer sigma + fourteen auditor quads (3584 B):** corpus `…_plus_fourteen_auditor_quads.hex`; harness **`test_layout_sigma_transfer_fourteen_auditor_quads_extension_byte_length_is_3584`** + **`test_deserialize_transfer_layout_extended_fourteen_auditors_ok_is_some`**; Lean **157–158** (`ldConst` **40**); **`deserializeSigmaTransfer26Scalars30PointsPlusFourteenAuditorQuadsBytes`** + prefix lemmas vs thirteen-quad tier; **`Refinement.Confidential`** **`sigma_transfer_ext3584_len_eval_eq`**. |
| 2026-04-12 | **Transfer sigma + fifteen / sixteen auditor quads (3712 B / 3840 B):** corpora `…_plus_fifteen_auditor_quads.hex` / `…_plus_sixteen_auditor_quads.hex`; harness length + **`deserialize_transfer`** `Some` rows; Lean **159–162** (`ldConst` **41–42**); **`deserializeSigmaTransfer26Scalars30PointsPlusFifteenAuditorQuadsBytes`** / **`…Sixteen…`** + prefix lemmas; **`Refinement.Confidential`** **`sigma_transfer_ext3712_len_eval_eq`** / **`sigma_transfer_ext3840_len_eval_eq`**. |
| 2026-04-12 | **Transfer sigma + seventeen / eighteen / nineteen auditor quads (3968 B / 4096 B / 4224 B):** corpora `…_plus_seventeen_auditor_quads.hex` / `…_plus_eighteen_auditor_quads.hex` / `…_plus_nineteen_auditor_quads.hex`; harness length + **`deserialize_transfer`** `Some` rows; Lean **163–168** (`ldConst` **43–45**); **`deserializeSigmaTransfer26Scalars30PointsPlus{Seventeen,Eighteen,Nineteen}AuditorQuadsBytes`** + prefix lemmas; **`Refinement.Confidential`** **`sigma_transfer_ext3968_len_eval_eq`** / **`sigma_transfer_ext4096_len_eval_eq`** / **`sigma_transfer_ext4224_len_eval_eq`**; **`DiffTest/Runner`** + **`ORACLE_CHANGELOG`** + inventory tables updated. |
| 2026-04-12 | **`fa_stub` suite:** second oracle row **`test_fa_stub_write_then_read_balance`** (`u64(9999)`); Lean **`Programs/Confidential`** index **169** (`faWriteBalance` + `faReadBalance` from empty **`faBalances`**); **`Refinement.Confidential`** **`fa_stub_write_then_read_balance_eval_eq_u64_9999`**; **`DiffTest/Runner`** mapping; **`STUB_POLICY`** L5 note. |
| 2026-04-10 | **Registration FS VM↔helpers:** `confidential_proof::registration_fs_message_for_test` promoted to normal **`public`** (removed `#[test_only]`); harness **`test_registration_fs_message_framework_matches_helpers_golden`**; Lean index **170** (`ldTrue`); **`Refinement.Confidential`** **`registration_fs_framework_matches_helpers_golden_eval_eq_true`**. |
| 2026-04-10 | **Production registration verify in harness:** `prove_registration_deterministic_for_difftest` + **`verify_registration_proof_for_difftest`** (`confidential_proof.move`); harness **`test_registration_proof_framework_deterministic_verify_roundtrip`**; helpers export **`registration_fixture_pubkey_from_secret_scalar`**; Lean **171** = **`caRegistrationHelpersRoundtripNative`** (same as **35**); **`Refinement.Confidential`** **`registration_helpers_roundtrip_eval_eq_framework_verify_roundtrip_eval`** (`BEq` **`==`** on `eval`); **`Tests.Confidential`** **`evalCA_171_eq_evalCA_35_fixture`**. |
| 2026-04-10 | **Second registration FS golden:** helpers **`registration_fs_message_golden_move_second_scenario`**; harness **`test_registration_fs_message_golden_move_second`** + **`test_registration_fs_message_framework_second_scenario_matches_helpers_golden`**; Lean **172–173** (const pool **46** + **`ldTrue`**); **`prove_registration`** refactored to call **`prove_registration_deterministic_for_difftest`**; **`Refinement.Confidential`** **`registration_fs_message_golden_move_second_eval_eq_vector`** / **`registration_fs_framework_second_scenario_matches_helpers_golden_eval_eq_true`**. |
| 2026-04-10 | **Second registration SHA2-512 corpus:** **`registration_sha2_512_golden_2.hex`** + **`verify-corpora`** Rust check; Lean **`TranscriptAlignment`** **`tagged_hash_golden2_msg_matches`** / length lemmas; **`Programs/Confidential`** **`registrationFsMsgGolden2MoveBytes_eq_expectedRegistrationFsMsg2_toList`**; Move audit **M2** hardening on **`new_scalar_from_sha2_512`**. |
| 2026-04-10 | **Oracle + L2:** harness **`test_registration_sha2_512_golden_move_{first,second}`** (via **`difftest_registration_helpers`**); Lean **`confidentialModuleEnv`** indices **174–175** (`ldConst` **47–48**); **`Refinement.Confidential`** **`registration_sha2_512_golden_move_*_eval_eq_vector`**. |
| 2026-04-12 | **Move audit notes:** [`CONFIDENTIAL_ASSETS_MOVE_AUDIT_NOTES.md`](../../CONFIDENTIAL_ASSETS_MOVE_AUDIT_NOTES.md) — static-review log for CA Move API semantics / harness preconditions (not product security sign-off); linked from CA plans, `lean/README`, `difftest/README`, `MoveModel/State.lean`. |
| 2026-04-12 | **`serialize_auditor_eks` non-empty:** `test_serialize_auditor_eks_single_a_point_framework` (32 B **A_POINT** wire); Lean **`funcIdx` 114** + const pool **10**; `oracle_row::vm_lean_row` refactor on layer suite; Move doc typo fixes (`sufficient`, `decrypt it`). |
| 2026-04-12 | **`serialize_auditor_amounts` non-empty:** `test_serialize_auditor_amounts_one_zero_pending_framework` (256 B all-zero wire); Lean **`funcIdx` 115** + const pool **11**; **`.meta.json`** + **`verify-corpora`** for serializer hex files. |
| 2026-04-12 | **Multi-element serializer wires:** `test_serialize_auditor_eks_two_a_points_framework` (**64** B), `test_serialize_auditor_amounts_two_zero_pending_framework` (**512** B); Lean **116–117**; const pool **12–13**; corpus + **`verify-corpora`** extended. |
| 2026-04-12 | **Serializer depth:** `test_serialize_auditor_amounts_one_u64_one_pending_framework` (**256** B non-trivial pending) + `test_serialize_auditor_amounts_one_actual_zero_framework` (**512** B); Lean **118–119**; const **14–15**; `serializeAuditorAmountsOneActualZeroWireBytes_eq_two_pending_zeros` (`native_decide`). |
| 2026-04-12 | **Mixed pending wire:** `test_serialize_auditor_amounts_zero_then_u64_one_framework` (**512** B = one-zero ‖ u64-one); Lean **120**; const **16**; `serializeAuditorAmountsZeroThenU64OneWireBytes`. |
| 2026-04-12 | **Reverse mixed wire:** `test_serialize_auditor_amounts_u64_one_then_zero_framework` (**512** B = u64-one ‖ one-zero); Lean **121**; const **17**; `serializeAuditorAmounts_mixed512_orders_distinct` (`native_decide`). |
| 2026-04-12 | **768 B mixed actual + u64 pending:** `test_serialize_auditor_amounts_actual_zero_then_u64_one_pending_framework` / `..._u64_one_pending_then_actual_zero_...` (uses **`u64(1)`** so byte-level order is visible — two all-zero pending/actual rows would coincide as **768** × `0u8`); Lean **122–123**; const **18–19**; `serializeAuditorAmounts_mixed768_orders_distinct`. |
| 2026-04-12 | **EK triple:** `test_serialize_auditor_eks_three_a_points_framework` (**96** B = 3×**A_POINT**); Lean **124**; const **20**; `serializeAuditorEksThreeApointWireBytes`. |
| 2026-04-12 | **EK quadruple:** `test_serialize_auditor_eks_four_a_points_framework` (**128** B = 4×**A_POINT**); Lean **125**; const **21**; `serializeAuditorEksFourApointWireBytes`. |
| 2026-04-12 | **EK quintuple:** `test_serialize_auditor_eks_five_a_points_framework` (**160** B = 5×**A_POINT**); Lean **126**; const **22**; `serializeAuditorEksFiveApointWireBytes` (+ `serializeAuditorEksFiveApointWireBytes_eq_deserializeRepeatConcat`). |
| 2026-04-10 | **EK sextuple:** `test_serialize_auditor_eks_six_a_points_framework` (**192** B = 6×**A_POINT**); Lean **127**; const **23**; `serializeAuditorEksSixApointWireBytes` + sigma-prefix lemmas (`deserializeSigma18Scalars18PointsBytes_six_points_eq_serializeAuditorEksSixApoint` and **19** / **transfer** variants). |
| 2026-04-10 | **Sigma wire length VM↔Lean:** `test_layout_sigma_18_scalars_18_points_byte_length_is_1152` / `…_19_…_1216` / `…_transfer_base_layout_…_1792` — Lean **128–130** (`ldConst` **24–26** + `vecLen` + `eq`). |
| 2026-04-12 | **Corpus verifier in Rust:** `move-lean-difftest verify-corpora` (+ `corpus_verify` unit test) is the **CI / `difftest.sh`** step for `corpora/confidential_assets/*.hex`. |
| 2026-04-12 | **Corpus tooling:** removed duplicate **`verify_registration_corpus.py`**; **`verify-corpora`** is the only supported checker for these hex goldens. |
| 2026-04-13 | E2e oracle **`confidential_transfer_rejects_mismatched_sender_recipient_amount_ciphertexts`** — VM **`MoveAbort`** **`65553`**; Lean **182**; **`Move.Step`** **`eval_bytecodeLdU64AbortModuleEnv_aborted_65553`**; **`Refinement.Confidential`** / **`Tests.Confidential`**; **`RunnerFuncMappingAux`**; inventory §8 row **`e2e_xfer_mismatch`**. |
| 2026-04-13 | E2e oracles **`confidential_transfer_rejects_when_recipient_frozen`** (**`196615`**, Lean **183**) and **`normalize_aborts_when_already_normalized_only`** (**`196619`**, Lean **184**); **`Move.Step`** **`eval_bytecodeLdU64AbortModuleEnv_aborted_{196615,196619}`**; **`Refinement.Confidential`** **`ca_e2e_abort_196615_*`** / **`ca_e2e_abort_196619_*`** / **`ca_e2e_abort_196615_196619_196616_eval_bundle`** (initially **196615**/**196619** only; **196616**/**185** added same day); **`Tests.Confidential`** **`evalCA_{183,184}_eq_eval`**; inventory §8 **`e2e_xfer_recipient_frozen`** / **`e2e_norm_twice`**. |
| 2026-04-13 | E2e oracles **`deposit_to_rejects_when_recipient_frozen`**, **`freeze_token_aborts_when_already_frozen_only`** (both **`196615`**, Lean **183**), **`unfreeze_token_aborts_when_not_frozen_only`** (**`196616`**, Lean **185**); **`Move.Step`** **`eval_bytecodeLdU64AbortModuleEnv_aborted_196616`**; **`Refinement.Confidential`** **`ca_e2e_abort_196616_*`** + **`ca_e2e_abort_196615_196619_196616_eval_bundle`** (replaces two-way bundle); **`Tests.Confidential`** **`evalCA_185_eq_eval`**; inventory §8 **`e2e_deposit_to_recipient_frozen`** / **`e2e_freeze_twice`** / **`e2e_unfreeze_not_frozen`**. |
| 2026-04-13 | Inventory §8 documents existing merged rows **`confidential_transfer_rejects_empty_auditors_when_asset_auditor_set`** / **`…_non_matching_asset_auditor_pubkey`** (**`65542`**, Lean **42**) as **`e2e_xfer_empty_auditors`** / **`e2e_xfer_wrong_asset_auditor_pk`**. E2e oracle **`deposit_rejects_when_account_frozen_self_deposit_only`** (**`196615`**, Lean **183**); **`RunnerFuncMappingAux`**; inventory **`e2e_deposit_self_when_frozen`**. FV: **`Refinement.Confidential`** **`ca_e2e_abort_65542_eval_eq_aborted`** / **`ca_e2e_abort_65542_65553_eval_bundle`**; **`Tests.Confidential`** **`evalCA_42_eq_eval`**; **`Move.Step`** doc blurb for minimal-abort stubs. |
| 2026-04-13 | E2e oracles **`register_aborts_when_store_already_published_only`** (**`524290`**, Lean **186**), **`rollover_pending_balance_aborts_when_denormalized_only`** (**`196618`**, Lean **187**), **`enable_token_aborts_when_already_enabled_only`** (**`196620`**, Lean **188**); **`Move.Step`** **`eval_bytecodeLdU64AbortModuleEnv_aborted_{524290,196618,196620}`**; **`Refinement.Confidential`** **`ca_e2e_abort_524290_*`** / **`ca_e2e_abort_196618_*`** / **`ca_e2e_abort_196620_*`** + **`ca_e2e_abort_524290_196618_196620_eval_bundle`**; **`Tests.Confidential`** **`evalCA_{186,187,188}_eq_eval`**; inventory §8 **`e2e_register_twice`** / **`e2e_rollover_twice_denorm`** / **`e2e_enable_token_twice`**; fragment helper **`bypass_outcome`** for VM-only **`enable_token`**. |
| 2026-04-13 | E2e oracles **`deposit_rejects_when_token_not_allowlisted_after_allow_list_enabled_only`** (**`65549`**, Lean **189**), **`enable_allow_list_aborts_when_already_enabled_only`** (**`196622`**, Lean **190**), **`disable_allow_list_aborts_when_already_disabled_only`** (**`196623`**, Lean **191**); **`confidential_asset_allow_list_governance_bypass_args`**; **`Move.Step`** **`eval_bytecodeLdU64AbortModuleEnv_aborted_{65549,196622,196623}`**; **`Refinement.Confidential`** **`ca_e2e_abort_65549_*`** / **`ca_e2e_abort_196622_*`** / **`ca_e2e_abort_196623_*`** + **`ca_e2e_abort_65549_196622_196623_eval_bundle`**; **`Tests.Confidential`** **`evalCA_{189,190,191}_eq_eval`**; inventory §8 **`e2e_deposit_token_disabled_allow_list`** / **`e2e_enable_allow_list_twice`** / **`e2e_disable_allow_list_twice`**. |
| 2026-04-13 | E2e oracles **`register_rejects_when_token_not_allowlisted_after_allow_list_enabled_first_only`** / **`deposit_rejects_after_disable_token_with_allow_list_on_only`** (both **`65549`**, Lean **189**), **`freeze_token_aborts_when_store_not_published_only`** (**`393219`**, Lean **192**), **`disable_token_aborts_when_already_disabled_only`** (**`196621`**, Lean **193**); **`confidential_asset_token_toggle_bypass_args`**; **`Move.Step`** **`eval_bytecodeLdU64AbortModuleEnv_aborted_{393219,196621}`**; **`Refinement.Confidential`** **`ca_e2e_abort_393219_*`** / **`ca_e2e_abort_196621_*`** + **`ca_e2e_abort_393219_196621_eval_bundle`**; **`Tests.Confidential`** **`evalCA_{192,193}_eq_eval`**; inventory §8 **`e2e_register_allow_list_before_token`** / **`e2e_deposit_after_disable_token_allow_list`** / **`e2e_freeze_token_store_not_published`** / **`e2e_disable_token_twice`**. |
| 2026-04-13 | E2e oracles **`unfreeze_token_aborts_when_store_not_published_only`**, **`rollover_pending_balance_aborts_when_store_not_published_only`**, **`rollover_pending_balance_and_freeze_aborts_when_store_not_published_only`** — all **`393219`**, Lean **192** (shared stub); **`RunnerFuncMappingAux`**; **`Refinement.Confidential`** **`ca_e2e_abort_393219_eval_eq_aborted_fuel30`** + **`ca_e2e_abort_393219_eval_fuel20_fuel30_agree`**; inventory §8 **`e2e_unfreeze_token_store_not_published`** / **`e2e_rollover_store_not_published`** / **`e2e_rollover_and_freeze_store_not_published`**. |
| 2026-04-17 | **Strong CA VM↔Lean rows — real latent bug caught + fixed.** Added **28** additional VM↔Lean rows (10 ElGamal, 18 balance-layer) that deliberately exercise *non-zero* plaintexts and *distinguishing* algebraic/structural properties. Until now every CA arithmetic row fed `scalar_zero()` / the zero pending balance, which silently hides a large class of copy-paste or identity-collapse regressions (all operators become indistinguishable on zero). Each new row returns `bool(true)` under the intended semantics and maps to the existing `funcIdx := 40` `ldTrue` witness — so a regression where the VM returns `false`/aborts produces an immediate mismatch with Lean and fails CI. **Real bug surfaced by this pass:** `confidential_balance::sub_balances_mut` was calling `twisted_elgamal::ciphertext_add_assign` instead of `ciphertext_sub_assign` (verbatim copy-paste from `add_balances_mut` 10 lines above); every pre-existing suite row used zero inputs so `sub(0, 0) = add(0, 0) = 0` passed trivially, and the bug had been latent since commit `f7d333986b` (#16384). The new rows `test_bal_sub_u64_one_from_u64_one_is_zero` / `test_bal_sub_u64_three_minus_two_equals_one` failed on the unfixed code (VM returned `false`/`false` because `sub(1,1)` became `add(1,1) = 2`, and `sub(3,2)` became `add(3,2) = 5`), exactly as predicted; the Move source was then corrected to call `ciphertext_sub_assign`, after which all 28 rows pass. The doc mirror `aptos-experimental/doc/confidential_balance.md` was updated in lockstep. **New ElGamal rows (10):** `test_elg_ciphertext_one_not_equal_zero`, `test_elg_ciphertext_one_bytes_differ_from_zero_bytes`, `test_elg_ciphertext_add_one_plus_zero_equals_one`, `test_elg_ciphertext_add_one_plus_two_equals_three`, `test_elg_ciphertext_sub_one_from_one_is_zero`, `test_elg_ciphertext_sub_three_minus_two_equals_one`, `test_elg_ciphertext_sub_assign_on_nonzero_matches_sub`, `test_elg_compress_decompress_nonzero_ciphertext_roundtrips`, `test_elg_get_value_component_nonzero_matches_basepoint_mul`, `test_elg_ciphertext_to_bytes_roundtrip_nonzero`. Each catches a concrete bug class: (in)equality collapse on unequal ciphertexts, byte-serialization ignoring the value component, `add`/`sub` being no-ops or swapped, compress/decompress losing the left point, `get_value_component` returning the wrong field, serializer that only round-trips zero. **New balance-layer rows (18):** `test_bal_different_u64_pending_not_equal`, `test_bal_different_u64_pending_c_not_equal`, `test_bal_plain_zero_not_equal_u64_one`, `test_bal_u64_large_not_zero` (`0xF000` in chunk 0), `test_bal_u64_high_chunk_not_zero` (`0xBEEF << 48` in chunk 3 — catches `is_zero_balance` short-circuiting on chunk 0 only), `test_bal_u64_one_bytes_differ_from_u64_two_bytes`, `test_bal_add_u64_one_plus_u64_two_equals_u64_three` (balance-layer homomorphic add on non-zero inputs), `test_bal_sub_u64_one_from_u64_one_is_zero` **(the row that caught the `sub_balances_mut` bug)**, `test_bal_sub_u64_three_minus_two_equals_one`, `test_bal_compress_decompress_nonzero_pending_equals_self`, `test_bal_pending_u64_one_bytes_roundtrip_equals_self`, `test_bal_pending_u64_one_bytes_contains_nonzero`, `test_bal_get_pending_chunks_is_four`, `test_bal_get_actual_chunks_is_eight`, `test_bal_get_chunk_bits_is_sixteen`, `test_bal_split_u64_zero_all_chunks_zero`, `test_bal_split_u128_zero_all_chunks_zero`, `test_bal_split_u64_one_only_first_chunk_nonzero`. **Runs (post-fix):** `./difftest.sh --suite confidential` → **200 passed / 0 failed** (up from 172); `DIFTEST_MERGE_CA_E2E=1 ./difftest.sh --suite confidential` → **368 passed / 0 failed** (up from 340); full `./difftest.sh` (all registered suites) → **687 passed / 0 failed**; `movement move test` on `aptos-experimental` CA modules → **64 / 64 pass**. `RunnerFuncMappingAux.lean` carries an inline explanation of the `ldTrue`-witness bug-catching pattern and the real-bug provenance for future readers. |
| 2026-04-17 | **Phase A gap-fill (CA `public fun` coverage).** Added three VM↔Lean rows covering previously-untested public functions in the ElGamal + CA-layer suites, all mapping to the existing `funcIdx := 40` `bool(true)` success-pin witness (same pattern as the ~65 merged CA e2e rows): **(i)** `test_elg_ciphertext_as_points_compress_equals_to_bytes` — exercises `ristretto255_twisted_elgamal::ciphertext_as_points` by compressing the two returned `&RistrettoPoint` refs and comparing to the canonical `ciphertext_to_bytes` wire; **(ii)** `test_elg_ciphertext_from_compressed_points_roundtrip` — builds a ciphertext via `ciphertext_from_compressed_points` from the slice halves of `ciphertext_to_bytes`, decompresses, and verifies `ciphertext_equals` against both the original and the `compress_ciphertext` round-trip; **(iii)** `test_layer_max_sender_auditor_hint_bytes_eq_256` — pins the `#[view] max_sender_auditor_hint_bytes()` constant to **256** (matches `MAX_SENDER_AUDITOR_HINT_BYTES`). **Runs:** `./difftest.sh --suite confidential` → **172 passed / 0 failed**; `DIFTEST_MERGE_CA_E2E=1 ./difftest.sh --suite confidential` → **340 passed / 0 failed**. **Harness-VM blocked natives (intentionally excluded).** `ristretto255::point_clone` is **not registered** in the `move-vm-test-utils` harness VM (aborts with `E_NATIVE_FUN_NOT_AVAILABLE` / canonical **196613**). This blocks VM↔Lean coverage for: `ristretto255_twisted_elgamal::ciphertext_clone`, `confidential_balance::balance_to_points_c`, `confidential_balance::balance_to_points_d` (and anything that transitively calls `point_clone`). Matching the harness-VM abort in Lean would record only the missing-native path (no real Move semantics) so those three candidates are deferred — they will be unblocked automatically when either `point_clone` is registered in `move-vm-test-utils` or the Lean evaluator models it as a native. **Test-only gaps (structurally un-addable from a 0x1 harness module).** `ristretto255_twisted_elgamal::{pubkey_from_secret_key, new_ciphertext, new_ciphertext_with_basepoint}`, `confidential_balance::{generate_balance_randomness, balance_randomness_as_scalars, new_actual_balance_from_u128, new_pending_balance_from_u64, verify_actual_balance, verify_pending_balance}`, `confidential_proof::{prove_*, serialize_{withdrawal,transfer,normalization,rotation}_proof, verify_registration_proof_for_test}`, and all `confidential_asset::assert_last_*_event` helpers are `#[test_only]` and cannot be called from the difftest harness module (`0x1::difftest_*`); unblocking would require promoting them to normal `public` (following the `verify_registration_proof_for_difftest` / `prove_registration_deterministic_for_difftest` precedent at rows **35** / **171**), which is a production-API surface change and is tracked under `CONFIDENTIAL_ASSETS_FORMAL_VERIFICATION_PLAN.md` Workstream A (not undertaken in this pass). **`verify_{withdrawal,transfer,normalization,rotation}_proof` remain Blocked** even with wrappers because their Lean columns require Ristretto point arithmetic, Bulletproofs verification, and a Fiat–Shamir sigma protocol modeled as natives — none currently exist in `MovementFormal.MoveModel`. `RunnerFuncMappingAux` carries an inline decision block documenting all three categories (harness-VM, test-only, proof-crypto) so future contributors don't re-open them. |
| 2026-04-17 | **Phase B++ follow-on — byte-layout / argument-order / compressed-constructor coherence rows (no additional latent bugs found).** Added **23** more VM↔Lean rows (10 ElGamal, 13 balance) targeting the next regression class beyond structural-equality-invisible bugs: **byte-level layout drift** and **argument-order transposition in constructors**. Rationale: `balance_equals` / `ciphertext_equals` compare as unordered points so a left/right-swap serializer that also invertibly swaps in the deserializer would round-trip — but the Fiat–Shamir transcript (which feeds `balance_to_bytes` / `ciphertext_to_bytes` / `pubkey_to_bytes` directly into `SHA2-512`) would compute a different challenge, silently invalidating every sigma proof. All rows map to `funcIdx := 40` (`ldTrue`). The new rows: **(i) ElGamal byte-layout pins** — `test_elg_ciphertext_to_bytes_first_32_is_left_basepoint` (first 32 B of `ciphertext_to_bytes(ct(1))` equals `basepoint_compressed()` — catches left/right serializer swap), `test_elg_ciphertext_to_bytes_last_32_is_right_identity` (last 32 B equals `point_identity_compressed()` — catches duplicate-left-into-both-halves bug), `test_elg_new_ciphertext_from_bytes_64_zero_is_identity_pair` (canonical 64-zero decodes to `(identity, identity)` — catches decoder rejecting valid identity encodings), `test_elg_ciphertext_from_bytes_basepoint_left_identity_right_roundtrip_bytes` (byte-equal roundtrip for `basepoint‖identity` input — catches silent canonical-reencoding drift). **(ii) ElGamal argument-order pins** — `test_elg_ciphertext_from_points_distinguishes_left_right` (`from_points(bp,id) != from_points(id,bp)` — catches struct-field swap), `test_elg_from_compressed_points_preserves_order` (`decompress(from_compressed_points(bp,id)) == ct(1)` — catches swap in the compressed variant), `test_elg_get_value_component_matches_into_points_left_nonzero` (`get_value_component(ct) == fst(into_points(ct))` on non-zero plaintext — catches `&ct.right` regression invisible under zero-randomness). **(iii) ElGamal equality reflexivity/commutativity on non-zero** — `test_elg_ciphertext_equals_reflexive_nonzero`, `test_elg_ciphertext_equals_commutative_on_distinct_nonzero`. **(iv) ElGamal pubkey byte-length** — `test_elg_pubkey_to_bytes_len_is_32`. **(v) Balance zero-canonical byte pins** — `test_bal_zero_pending_bytes_all_zero` (256 zero bytes exactly), `test_bal_zero_actual_bytes_all_zero` (512 zero bytes exactly). **(vi) Balance non-zero byte-layout pin** — `test_bal_pending_u64_one_byte_layout` (first 32 B of `balance_to_bytes(pending_u64(1))` equals `basepoint_compressed()`, all remaining 224 B zero — catches chunk reorder, per-chunk left/right swap, big-endian chunk ordering). **(vii) Balance decoder validation pins** — `test_bal_pending_from_bytes_invalid_chunk0_left_is_none` (invalid `0xff*32` at chunk-0 left rejected) and `test_bal_pending_from_bytes_invalid_chunk3_right_is_none` (invalid at chunk-3 right rejected — catches early-exit validation loop). **(viii) Compressed-constructor coherence pins** — `test_bal_compressed_pending_no_rand_matches_plain` and `test_bal_compressed_actual_no_rand_matches_plain` (`decompress(new_compressed_*_balance_no_randomness()) balance_equals new_*_balance_no_randomness()` — `register_internal` uses the compressed variant on-chain so a silent drift here would stamp bad initial state). **(ix) Canonical-zero decoder coherence** — `test_bal_new_pending_from_256_zeros_equals_plain_zero` and `test_bal_new_actual_from_512_zeros_equals_plain_zero`. **(x) Compress/decompress byte roundtrip on non-zero** — `test_bal_compress_decompress_bytes_roundtrip_u64_seven` (byte-identical roundtrip on `pending_u64(7)`). **(xi) Accumulator non-zero identity** — `test_bal_add_zero_plus_nonzero_equals_nonzero` (catches `add_balances_mut` failing to write through the mutable reference). **(xii) `balance_c_equals` / `balance_equals` discriminator on distinct non-zero** — `test_bal_c_equals_on_distinct_u64_is_false` and `test_bal_equals_commutative_distinct` (pair with the earlier C-only discriminator at a different input). **Lean mapping layout:** all 23 entries added to `funcNameToMappingPart7` (no new `Part` needed; still well under the `maxHeartbeats := 200000` budget). **Runs (post-addition):** `./difftest.sh --suite confidential` → **279 passed / 0 failed** (up from 256); `DIFTEST_MERGE_CA_E2E=1 ./difftest.sh --suite confidential` → **447 passed / 0 failed** (up from 424); full `./difftest.sh` (all registered suites) → **766 passed / 0 failed** (up from 743); `movement move test` on `aptos-experimental` → **64 / 64 pass** (unchanged — the Move source was untouched in Phase B++). No additional production bugs were surfaced — every new row passes on the current CA source. Running bug-fixes log: [`inventory/bug_fixes_found_by_difftests.md`](bug_fixes_found_by_difftests.md). |
| 2026-04-17 | **Phase B+ follow-on — operand-swap / accumulator no-op regression rows (no additional latent bugs found).** Added **13** more VM↔Lean rows targeting the specific regression classes that originally hid the `sub_balances_mut` bug but at *different* call sites — i.e. regression coverage for "what if the same class of copy-paste slip happens next time, but in `ciphertext_add` / `add_balances_mut` / the chunk splitter?". All rows map to `funcIdx := 40` (`ldTrue`) so a VM regression flips the row to `bool(false)` and fails CI. The new rows: **(i) ElGamal operand-swap and accumulator tests** — `test_elg_ciphertext_sub_not_commutative_on_distinct_nonzero` (`sub(ct(3), ct(2)) != sub(ct(2), ct(3))` — catches `point_sub` accidentally symmetrized), `test_elg_ciphertext_sub_five_minus_three_equals_two_nonzero` (direct `5 - 3 == 2` pin), `test_elg_ciphertext_add_assign_accumulates_three_nonzero` (`acc=ct(1); acc += ct(2); acc += ct(3); acc == ct(6)` — catches no-op `add_assign`), `test_elg_ciphertext_sub_assign_chain_nonzero` (`acc=ct(10); acc -= ct(3); acc -= ct(2); acc == ct(5)` — direct ElGamal-layer regression coverage for the `sub_balances_mut` bug class), `test_elg_ciphertext_add_sub_distinct_nonzero` (`add(ct(3),ct(2)) != sub(ct(3),ct(2))` — catches `ciphertext_add` accidentally aliasing `ciphertext_sub`), `test_elg_compress_decompress_ciphertext_0xffff_and_len` (compress/decompress roundtrip with plaintext `0xffff` = chunk-boundary max + byte-length pin). **(ii) Balance-layer operand-swap tests** — `test_bal_add_vs_sub_distinct_nonzero` (`add_balances_mut(3,2)` ≠ `sub_balances_mut(3,2)` — direct regression for the actual Bug #1 call site; even on the fixed code the two compute `bal(5)` vs `bal(1)` respectively), `test_bal_sub_not_commutative_on_distinct_nonzero` (`sub(bal(5), bal(3)) != sub(bal(3), bal(5))`), `test_bal_add_balances_mut_accumulates_three` (three-way add-mut accumulator, catches no-op mutator), `test_bal_sub_balances_mut_chain_nonzero` (three-way sub-mut accumulator, catches add/no-op regression). **(iii) Splitter top-chunk pins** — `test_bal_split_u64_max_all_chunks_ffff` (feeds `0xffff_ffff_ffff_ffff` and asserts every one of the 4 chunks equals `0xffff` — catches wrong-mask `& 0xff` or high-chunk drop), `test_bal_split_u128_top_chunk_ffff_only` (feeds `0xffff << 112` and asserts only chunk 7 is `0xffff` with all other 7 chunks zero — catches off-by-one on the top chunk). **(iv) Byte-exact roundtrip** — `test_bal_pending_u64_three_bytes_roundtrip_byte_equals` (`balance_to_bytes` → `new_pending_balance_from_bytes` → `balance_to_bytes` produces byte-identical output for a non-zero balance; stronger than `balance_equals` which only checks ciphertext points). **Lean mapping layout:** new entries added to a dedicated `funcNameToMappingPart7` alongside `Part6` to keep each private match under the default Lean `maxHeartbeats := 200000`. **Runs (post-addition):** `./difftest.sh --suite confidential` → **256 passed / 0 failed** (up from 243); `DIFTEST_MERGE_CA_E2E=1 ./difftest.sh --suite confidential` → **424 passed / 0 failed** (up from 411); full `./difftest.sh` (all registered suites) → **743 passed / 0 failed** (up from 730); `movement move test` on `aptos-experimental` → **64 / 64 pass** (unchanged — the Move source was untouched in Phase B+). No additional production bugs were surfaced — every new row passes on the current CA source. The running bug-fixes log lives at [`inventory/bug_fixes_found_by_difftests.md`](bug_fixes_found_by_difftests.md). |
| 2026-04-17 | **Phase C — deserializer reject/accept pins for the four confidential-proof types (no additional latent bugs found).** Added **8** more VM↔Lean rows all mapping to `funcIdx := 40` (`ldTrue`) pinning explicit reject / accept behavior of `deserialize_{withdrawal,transfer,normalization,rotation}_proof` on crafted byte inputs. The full prove → serialize → deserialize → verify happy-path round-trip is **architecturally blocked** from a non-test-only harness module because `ristretto255_bulletproofs::prove_batch_range_pedersen` (and the transitive `confidential_proof::prove_*`, `confidential_balance::generate_balance_randomness`, `confidential_balance::new_actual_balance_from_u128`, `confidential_balance::balance_randomness_as_scalars`, `ristretto255_twisted_elgamal::new_ciphertext_with_basepoint`) are all `#[test_only]` in aptos-stdlib / aptos-experimental; a non-`#[test_only]` caller cannot name `#[test_only]` callees even when harness compilation has `testing: true`. Covered in lieu: (i) **length-reject** rows — `test_deserialize_{withdrawal,normalization,rotation,transfer}_proof_short_sigma_is_none` feed 0-3 bytes and require `Option::is_none`; (ii) **off-by-one length-reject** rows — `test_deserialize_{withdrawal,normalization}_proof_one_byte_short_is_none` feed 1151 bytes (one byte short of the `18 scalars + 18 points = 36 * 32 = 1152 B` witness shape) and `test_deserialize_rotation_proof_one_byte_short_is_none` feeds 1183 bytes (one byte short of the rotation `37 * 32 = 1184 B` shape); any decoder that accepts arbitrary-length sigma bytes (e.g. truncate-and-zero-extend) flips these to `is_some`; (iii) **structural-accept** row — `test_deserialize_normalization_proof_all_zero_sigma_is_some` feeds exactly 1152 zero bytes and requires `Option::is_some` because the all-zero witness is the scalar-zero + identity-point decoding, which must be accepted by a spec-correct decoder; a regression that tightens the decoder to reject zero points (e.g. mistakenly using `point_decompress` then checking for identity) would flip this to `is_none`. Mapping: all 8 rows wired through a dedicated `funcNameToMappingPart8` in `RunnerFuncMappingAux.lean` with explicit inline documentation of the architectural block. Runs (post-addition): `./difftest.sh --suite confidential_proof` → **93 passed / 0 failed** (up from 85); `./difftest.sh --suite confidential` → **287 passed / 0 failed** (up from 279); `DIFTEST_MERGE_CA_E2E=1 ./difftest.sh --suite confidential` → **455 passed / 0 failed** (up from 447); full `./difftest.sh` (all registered suites) → **774 passed / 0 failed** (up from 766); `cargo test -p aptos-framework --test move_unit_test move_experimental_unit_tests` → **64 / 64 pass** (unchanged — the Move source was untouched in Phase C). In lockstep with this phase the inventory now carries a **§8.1 function-by-function coverage matrix** covering every public function in `ristretto255_twisted_elgamal`, `confidential_balance`, `confidential_proof`, `confidential_asset`, and `confidential_gas_e2e_helpers`, tagging each as **✓** / **BLOCKED(harness)** / **BLOCKED(Lean)** / **Gated on Phase C promotion** / **`#[test_only]`** / **friend-only** / **storage-dependent / e2e only**, with the precise unblocker for each bucket — this makes the gap set permanent and reviewable instead of living in conversation. |
| 2026-04-17 | **Phase D.1 — negative-direction VM↔Lean pins for the four `verify_*_proof` functions (no latent bugs found).** The four full `verify_*_proof` happy paths (`verify_withdrawal_proof`, `verify_transfer_proof`, `verify_normalization_proof`, `verify_rotation_proof`) have been **Blocked** on both sides since Phase A: (a) from a non-`#[test_only]` harness module we cannot name the `#[test_only]` provers (`confidential_proof::prove_*`, `confidential_balance::generate_balance_randomness`, `ristretto255_twisted_elgamal::new_ciphertext_with_basepoint`, `ristretto255_bulletproofs::prove_batch_range_pedersen`, …) that would let us build a valid-proof round-trip, and (b) the Lean side would additionally need `ristretto255::{scalar_mul, point_mul, point_add, point_sub, new_scalar_from_sha2_512}` + `ristretto255_bulletproofs::verify_batch_range_proof` modeled as natives before we could match the VM's accept branch on a real proof. This phase pins the *reject* branch — the one that actually matters for preventing "the proof check got silently disabled / short-circuited to `true` / skipped" regressions in production — without needing any of the above. Added **4** new VM↔Lean rows: `test_verify_{withdrawal,normalization,rotation,transfer}_proof_zero_sigma_aborts`. Each row (i) uses `deserialize_{withdrawal,normalization,rotation,transfer}_proof` to parse a length-valid all-zero sigma (**1152** / **1152** / **1184** / **1792** B) + an empty bulletproofs blob, then (ii) calls the real non-test `verify_*_proof` with deterministic chain-ID / address / secret-scalar inputs and a zero pending/actual balance that forces the sigma protocol's MSM equality checks to fail. The VM aborts with the canonical `ESIGMA_PROTOCOL_VERIFY_FAILED` = `error::invalid_argument(1)` = **65537**. Lean matches this with a new `caSigmaVerifyFailedAbortDesc` (`ldU64 65537 ; abort_`) pinned at `funcIdx := 195`, wired through a fresh `funcNameToMappingPart9` in `RunnerFuncMappingAux.lean`. A regression that short-circuits `verify_*_sigma_proof` to return `true` (or bypasses it) would flip the VM row from `Aborted(65537)` to `Ok` and the oracle would diverge from Lean immediately. **Harness-VM feature-flag fix (pre-existing latent harness gap, surfaced & fixed).** The first run of the four new rows hit `MoveAbort(196613 = E_NATIVE_FUN_NOT_AVAILABLE)` before reaching the sigma check: `verify_*_sigma_proof` → `confidential_balance::balance_to_points_{c,d}` → `ristretto255::point_clone` was gated by `features::bulletproofs_enabled()`, which reads the **on-chain `0x1::features::Features` resource**, not the Rust `aptos_natives` side (which already enables the flag). The difftest harness was only injecting `SHA_512_AND_RIPEMD_160_FEATURE_ID` (25) into the on-chain resource. Fix in `aptos-move/framework/formal/difftest/src/vm.rs`: added constants `BULLETPROOFS_NATIVES_FEATURE_ID = 24` / `BULLETPROOFS_BATCH_NATIVES_FEATURE_ID = 87` and extended `ensure_sha512_move_stdlib_feature` to merge both bits into the on-chain resource bitset. This unblocks *every* future CA row that transitively touches `point_clone` / `double_scalar_mul` / Bulletproofs verify natives (e.g. happy-path Phase D.2/D.3 proofs, `ciphertext_clone`, `balance_to_points_c/d` which were flagged under Phase A as "harness-VM blocked"). No other harness or Lean changes; Move source untouched. **Runs (post-addition):** `./difftest.sh --suite confidential_proof` → **97 passed / 0 failed** (up from 93); `./difftest.sh --suite confidential` (non-merged) → same delta; `DIFTEST_MERGE_CA_E2E=1 ./difftest.sh --suite confidential` → **459 passed / 0 failed** (up from 455); full `./difftest.sh` (all registered suites) → **778 passed / 0 failed** (up from 774); `cargo test -p aptos-framework --test move_unit_test --release` — all **45** `0x7::confidential_{proof_tests,asset_tests,balance_tests}` tests pass (one pre-existing `0x1::staking_config` failure unrelated to this work / the CA source tree). **§8.1 coverage-matrix rows** for `verify_withdrawal_proof` / `verify_transfer_proof` / `verify_normalization_proof` / `verify_rotation_proof` upgraded from **BLOCKED(Lean)** to **negative-pin only (Lean witness 195)** with the precise unblocker (Phase D.2/D.3 bakes offline-generated valid proof bytes as `const vector[…]` into the harness; Lean side still deferred until Ristretto + BP natives are modeled). The running bug-fixes log at [`inventory/bug_fixes_found_by_difftests.md`](bug_fixes_found_by_difftests.md) records the harness feature-flag gap (Bug #2 — harness-infrastructure, not production). |
| 2026-04-17 | **Phase B follow-on — further strong bug-catching CA rows (no additional latent bugs found; all rows confirm correct semantics).** Added **31** more VM↔Lean rows across the four CA suites, all mapping to `funcIdx := 40` (`ldTrue`) so a regression in the Move source (not Lean) flips the VM result to `false` and fails CI. Rationale: the Phase A "Strong rows" pass exposed one real bug (`sub_balances_mut`). Phase B closes the remaining low-likelihood-but-high-severity regression classes surfaced by the CA public-function audit in [`inventory/bug_fixes_found_by_difftests.md`](bug_fixes_found_by_difftests.md). The new rows split into five families: **(1) Splitter operator-precedence pins** — `test_bal_split_u64_65536_chunk0_is_zero`, `test_bal_split_u64_65537_chunk0_is_one`, `test_bal_split_u128_65536_chunk0_is_zero_chunk1_is_one`, `test_bal_split_u64_0xffff_chunk0_is_0xffff`. These feed the splitter inputs where a missing `& 0xffff` after the `>>` would leave high bits in chunk 0; each row asserts chunk 0 matches the masked (not raw-shifted) value. **(2) `is_zero_balance` chunk-by-chunk coverage** — `test_bal_u64_chunk1_only_not_zero`, `test_bal_u64_chunk2_only_not_zero`, `test_bal_u64_chunk2_only_not_zero_strict`. Each places mass exclusively in one higher chunk so a regression to `is_zero_balance` that only scans `chunks[0]` (or skips `chunks[2]`) flips the result. **(3) `balance_equals` ≠ `balance_c_equals` discriminator** — `test_bal_c_equals_but_not_equals_when_only_d_differs`. Builds a "weird" pending balance via `new_pending_balance_from_bytes` where every chunk has `C = identity` (32 zero bytes) but `D = A_POINT`, and asserts simultaneously `balance_c_equals(zero, weird) == true` AND `balance_equals(zero, weird) == false`. A regression that makes `balance_c_equals` also inspect `D` (or `balance_equals` ignore `D`) flips one side of the conjunction and the row fails. **(4) Homomorphic algebra on non-zero balances** — `test_bal_add_commutes_nonzero`, `test_bal_add_associative_nonzero`, `test_bal_add_zero_rhs_preserves_nonzero_lhs`, `test_bal_sub_zero_rhs_preserves_nonzero_lhs`, `test_bal_add_then_sub_recovers_original`, `test_bal_bytes_chunk_order_matters`; at the ElGamal layer `test_elg_ciphertext_add_associative_nonzero`, `test_elg_ciphertext_add_commutative_nonzero`, `test_elg_ciphertext_sub_assign_self_is_zero_nonzero`, `test_elg_ciphertext_add_assign_one_plus_two_equals_three`, `test_elg_ciphertext_add_then_sub_recovers_original_nonzero`, `test_elg_ciphertext_to_bytes_len_64_nonzero`, `test_elg_get_value_component_not_identity_when_v_nonzero`. These pin commutativity/associativity/identity/round-trip semantics under **non-trivial** inputs so a silent re-emergence of the Phase-A `sub_balances_mut` bug (or the sibling `add_balances_mut` dual) would be caught by multiple rows simultaneously. **(5) Fiat-Shamir DST domain separation** — `test_fs_dst_{transfer_not_equal_rotation, withdrawal_not_equal_normalization, registration_not_equal_normalization, withdrawal_not_equal_transfer, withdrawal_not_equal_rotation, withdrawal_not_equal_registration, transfer_not_equal_normalization, transfer_not_equal_registration, rotation_not_equal_normalization, rotation_not_equal_registration, bulletproofs_not_equal_any_sigma_dst}` — pairwise inequality pins that specifically catch a same-length copy-paste swap between `get_fiat_shamir_transfer_sigma_dst` and `get_fiat_shamir_rotation_sigma_dst` (both 34 B) which would otherwise slip every length-only pin. Plus the **exact-literal pins**: `test_fs_dst_{transfer,rotation,withdrawal,normalization,registration,bulletproofs}_bytes_exact` + `test_bulletproofs_num_bits_is_16`. A getter returning the wrong-but-correct-length literal is caught here. **(6) Auditor-serialization order** — `test_serialize_auditor_eks_order_matters` (`A_POINT` and `basepoint_compressed()` — two distinct valid Ristretto points — swapping the vector order must change the bytes), `test_serialize_auditor_eks_single_a_point_bytes_are_a_point` (exact 32-byte pin), `test_serialize_auditor_amounts_u64_one_differs_from_zero` (amount encodes differently), `test_serialize_auditor_amounts_order_matters`. Catches sort/dedupe/reverse regressions in the serializer. **Lean mapping layout:** new entries added to a dedicated `funcNameToMappingPart6` to keep each private match under the default Lean `maxHeartbeats := 200000` (same reason Part1..Part5 exist). **Runs (post-addition):** `./difftest.sh --suite confidential` → **243 passed / 0 failed** (up from 200); `DIFTEST_MERGE_CA_E2E=1 ./difftest.sh --suite confidential` → **411 passed / 0 failed** (up from 368); full `./difftest.sh` (all registered suites) → **730 passed / 0 failed** (up from 687); `movement move test` on `aptos-experimental` → **64 / 64 pass** (unchanged — the Move source was untouched in Phase B). No additional production bugs were surfaced by Phase B — every new row passes on the current `confidential_balance.move` / `ristretto255_twisted_elgamal.move` / `confidential_proof.move` / `confidential_asset.move`. The dedicated bug-fixes log lives at [`inventory/bug_fixes_found_by_difftests.md`](bug_fixes_found_by_difftests.md) and lists every production fix made during the CA difftest work (currently one: `sub_balances_mut`). |
