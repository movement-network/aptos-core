import MovementFormal.Experimental.ConfidentialAsset.Registration.EvalEquiv.Part2A
import MovementFormal.Experimental.ConfidentialAsset.Registration.EvalEquiv.Part2B
import MovementFormal.Experimental.ConfidentialAsset.Registration.EvalEquiv.Part2C

/-!
# Part2 — aggregator

`Part2.lean` was split at PC 44 and PC 55 (see `README.md` under "Split considerations"). The
actual content now lives in `Part2A.lean` (PCs 31–43), `Part2B.lean` (PCs 44–54), and
`Part2C.lean` (PCs 55–62). This file just re-exports them so `import Part2` still works.
-/
