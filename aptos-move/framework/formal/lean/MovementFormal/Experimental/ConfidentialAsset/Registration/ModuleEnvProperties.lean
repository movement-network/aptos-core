import MovementFormal.MoveModel.Value
import MovementFormal.MoveModel.State
import MovementFormal.MoveModel.Native.Registration
import MovementFormal.MoveModel.Programs.Registration

/-!
# Module Environment Properties (deprecated stub)

This file previously held three axioms:
- `IsWellFormedRegistrationEnv : ModuleEnv → Prop`
- `registrationModuleEnv (o : RegistrationNativeOracle) : ModuleEnv` (shadowing the real def)
- `registrationModuleEnv_wellformed`

None were referenced outside this file (only `FrameWellFormedness.lean` imports the file
itself, but does not use any of the three axioms). They are removed; the import edge is
preserved via the empty stub.
-/

namespace MovementFormal.Experimental.ConfidentialAsset.Registration.ModuleEnvProperties

end MovementFormal.Experimental.ConfidentialAsset.Registration.ModuleEnvProperties
