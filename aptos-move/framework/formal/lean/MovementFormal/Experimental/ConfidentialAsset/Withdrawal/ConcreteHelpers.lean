import MovementFormal.MoveModel.Programs.Withdrawal
import MovementFormal.MoveModel.StepLemmas.Run
import MovementFormal.MoveModel.ExecResultDropMs
import MovementFormal.Experimental.ConfidentialAsset.Helpers.ArgumentMarshaling
import MovementFormal.Experimental.ConfidentialAsset.Helpers.OracleComposition

/-!
# Withdrawal Concrete Proof Helpers (deprecated stub)

This file previously held per-PC `axiom` declarations modelling the bytecode behaviour
of the Withdrawal crypto verifier. None of those axioms were ever referenced (only mentioned in
comments inside `EvalEquiv.lean` files). The file is kept as an empty stub to preserve the
import edges; the body is intentionally vacuous so the axiom liability is removed from
`audit/AXIOM_INVENTORY.md`.
-/

namespace MovementFormal.Experimental.ConfidentialAsset.Withdrawal.ConcreteHelpers

end MovementFormal.Experimental.ConfidentialAsset.Withdrawal.ConcreteHelpers
